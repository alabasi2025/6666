# 🤝 دليل المساهمة

مرحباً بك في مشروع نظام إدارة الطاقة المتكامل! نحن سعداء باهتمامك بالمساهمة.

## 📋 القواعد الصارمة

قبل المساهمة، يجب قراءة والالتزام بـ [القواعد الصارمة](../القواعد_الصارمة.md).

## 🚀 البدء السريع

### 1. استنساخ المستودع
```bash
git clone https://github.com/alabasi2025/6666.git
cd 6666
```

### 2. تثبيت التبعيات
```bash
pnpm install
```

### 3. إعداد البيئة
```bash
cp .env.example .env
# قم بتعديل .env بالإعدادات المناسبة
```

### 4. تشغيل قاعدة البيانات
```bash
pnpm db:push
```

### 5. تشغيل التطبيق
```bash
pnpm dev
```

## 📝 عملية المساهمة

### 1. إنشاء فرع جديد
```bash
git checkout -b feature/اسم-الميزة
# أو
git checkout -b fix/اسم-الإصلاح
```

### 2. البحث قبل البناء (القاعدة 15)
قبل إنشاء أي ميزة جديدة:
- ابحث في الكود عن ميزات مشابهة
- تحقق من الـ Issues المفتوحة
- اسأل في المناقشات إذا لم تكن متأكداً

### 3. الالتزام بالقواعد
- لا أسرار في الكود
- لا `console.log` (استخدم Logger)
- لا `@ts-nocheck` بدون سبب
- ملفات أقل من 500 سطر

### 4. الاختبار
```bash
pnpm test
pnpm check
```

### 5. إنشاء Pull Request
- استخدم قالب PR المتوفر
- أكمل قائمة التحقق
- اربط الـ Issue المتعلقة

## 📁 هيكل المشروع

```
├── client/          # الواجهة الأمامية (React)
├── server/          # الواجهة الخلفية (Express)
│   ├── db/          # دوال قاعدة البيانات
│   ├── middleware/  # الوسطاء
│   └── utils/       # أدوات مساعدة
├── drizzle/         # مخططات قاعدة البيانات
└── docs/            # التوثيق
```

## 🔍 مراجعة الكود

كل PR يجب أن:
1. يمر من فحوصات CI/CD
2. يُراجع من شخص واحد على الأقل
3. يلتزم بالقواعد الصارمة

## 📞 التواصل

- **Issues**: للأخطاء والميزات
- **Discussions**: للأسئلة والنقاشات
