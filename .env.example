# قاعدة البيانات
DATABASE_URL=mysql://user:password@localhost:3306/energy_management

# إعدادات الخادم
PORT=5000
HOST=0.0.0.0
NODE_ENV=development

# إعدادات المدير الافتراضي (يجب تغييرها في الإنتاج)
DEFAULT_ADMIN_PHONE=0500000000
DEFAULT_ADMIN_PASSWORD=your_secure_password_here
DEFAULT_ADMIN_NAME=مدير النظام

# مستوى التسجيل (debug, info, warn, error)
LOG_LEVEL=info

# إعدادات الجلسة
SESSION_SECRET=your_session_secret_here

# إعدادات OAuth (اختياري)
# OAUTH_CLIENT_ID=
# OAUTH_CLIENT_SECRET=
