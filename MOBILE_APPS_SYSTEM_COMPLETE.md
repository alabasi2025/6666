# 📱 نظام تطبيقات الجوال - Mobile Apps System

## ✅ ما تم إنجازه

### 1. **قاعدة البيانات (Database Schema)**
- ✅ إنشاء `drizzle/schemas/mobile-apps.ts` مع الجداول:
  - `mobile_apps` - التطبيقات
  - `mobile_app_screens` - الشاشات
  - `mobile_app_features` - الوظائف
  - `mobile_app_permissions` - الصلاحيات
  - `user_mobile_app_access` - وصول المستخدمين

### 2. **Frontend Pages**
- ✅ `MobileAppsManagement.tsx` - إدارة التطبيقات
- ✅ `CustomerAppScreens.tsx` - إدارة شاشات تطبيق العميل (9 شاشات)
- ✅ `EmployeeAppScreens.tsx` - إدارة شاشات تطبيق الموظف (13 شاشة)
- ✅ `MobileAppPermissions.tsx` - إدارة الصلاحيات
- ✅ `UserMobileAccess.tsx` - إدارة وصول المستخدمين

### 3. **Navigation Integration**
- ✅ إضافة قسم "تطبيقات الجوال" في `Dashboard.tsx`
- ✅ إضافة Routes للصفحات الجديدة
- ✅ Lazy loading للصفحات

---

## 📱 تطبيق العميل (Customer App)

### **الشاشات (9 شاشات):**

1. **لوحة التحكم** (`/customer/dashboard`)
   - عرض الرصيد المستحق
   - آخر فاتورة
   - آخر قراءة
   - إشعارات
   - إحصائيات سريعة

2. **الفواتير** (`/customer/invoices`)
   - قائمة الفواتير
   - تفاصيل الفاتورة
   - تحميل PDF
   - مشاركة
   - دفع الفاتورة

3. **المدفوعات** (`/customer/payments`)
   - سجل المدفوعات
   - تفاصيل الدفعة
   - تحميل الإيصال
   - دفع فاتورة

4. **العدادات** (`/customer/meters`)
   - قائمة العدادات
   - تفاصيل العداد
   - سجل القراءات
   - إشعارات

5. **القراءات** (`/customer/readings`)
   - عرض القراءات
   - إدخال قراءة يدوية
   - مقارنة الاستهلاك

6. **المحفظة** (`/customer/wallet`)
   - عرض الرصيد
   - شحن المحفظة
   - سجل المعاملات
   - استخدام للدفع

7. **الشكاوى** (`/customer/complaints`)
   - تقديم شكوى
   - متابعة الشكاوى
   - إرفاق ملفات

8. **الملف الشخصي** (`/customer/profile`)
   - عرض/تعديل البيانات
   - تغيير كلمة المرور
   - إعدادات الإشعارات

9. **الإشعارات** (`/customer/notifications`)
   - قائمة الإشعارات
   - قراءة/عدم قراءة
   - إعدادات

---

## 👷 تطبيق الموظف (Employee App)

### **الشاشات (13 شاشة):**

1. **لوحة التحكم** (`/employee/dashboard`)
   - المهام اليومية
   - المهام المعلقة
   - الإحصائيات
   - الخريطة

2. **المهام** (`/employee/tasks`)
   - قائمة المهام
   - تفاصيل المهمة
   - بدء/إيقاف/إتمام
   - تحديث الحالة
   - رفع صور

3. **القراءات** (`/employee/readings`)
   - قائمة العدادات
   - إدخال قراءة
   - مسح QR
   - رفع صورة
   - GPS

4. **التركيبات** (`/employee/installations`)
   - قائمة عمليات التركيب
   - تسجيل بيانات العداد
   - تسجيل الختومات
   - تسجيل القواطع
   - القراءة الأولية
   - الصور (قبل/بعد)
   - GPS

5. **الاستبدالات** (`/employee/replacements`)
   - قائمة عمليات الاستبدال
   - تسجيل العداد القديم
   - تسجيل العداد الجديد
   - سبب الاستبدال
   - الصور

6. **الفصل/الربط** (`/employee/disconnections`)
   - قائمة العمليات
   - تسجيل السبب
   - حالة العداد
   - الصور

7. **الصيانة** (`/employee/maintenance`)
   - قائمة أعمال الصيانة
   - نوع الصيانة
   - الأجزاء المستبدلة
   - الوقت المستغرق
   - الصور

8. **الفحص الميداني** (`/employee/inspection`)
   - قائمة عمليات الفحص
   - نموذج الفحص
   - نتائج الفحص
   - الصور

9. **التحصيل** (`/employee/collection`)
   - قائمة الفواتير
   - تحصيل المدفوعات
   - إصدار إيصال
   - تحديث الحالة

10. **المواد** (`/employee/materials`)
    - طلب مواد
    - استلام مواد
    - إرجاع مواد

11. **الموقع** (`/employee/location`)
    - تتبع الموقع
    - عرض المهام على الخريطة
    - التنقل
    - تسجيل موقع

12. **الملف الشخصي** (`/employee/profile`)
    - عرض/تعديل البيانات
    - تغيير كلمة المرور
    - إعدادات

13. **الإشعارات** (`/employee/notifications`)
    - قائمة الإشعارات
    - إشعارات المهام
    - إعدادات

---

## 🔐 نظام الصلاحيات

### **تطبيق العميل (11 صلاحية):**
- `customer:view_dashboard`
- `customer:view_invoices`
- `customer:view_payments`
- `customer:view_meters`
- `customer:view_readings`
- `customer:view_wallet`
- `customer:charge_wallet`
- `customer:pay_invoice`
- `customer:create_complaint`
- `customer:view_complaints`
- `customer:update_profile`

### **تطبيق الموظف (17 صلاحية):**
- `worker:view_dashboard`
- `worker:view_tasks`
- `worker:start_task`
- `worker:complete_task`
- `worker:read_meter`
- `worker:install_meter`
- `worker:replace_meter`
- `worker:disconnect_meter`
- `worker:reconnect_meter`
- `worker:maintain_meter`
- `worker:inspect_meter`
- `worker:collect_payment`
- `worker:request_materials`
- `worker:receive_materials`
- `worker:update_location`
- `worker:upload_photos`
- `worker:update_profile`

---

## 📋 الخطوات التالية

### **قاعدة البيانات:**
- [ ] إنشاء Migration للجداول الجديدة
- [ ] Seed بيانات التطبيقات (تطبيق العميل، تطبيق الموظف)
- [ ] Seed بيانات الشاشات (9 شاشات للعميل، 13 شاشة للموظف)
- [ ] Seed بيانات الصلاحيات (11 للعميل، 17 للموظف)

### **Backend:**
- [ ] إنشاء `mobileAppsRouter.ts`
- [ ] إضافة Router إلى `routers.ts`
- [ ] إنشاء Procedures:
  - Apps Management (CRUD)
  - Screens Management (CRUD)
  - Features Management (CRUD)
  - Permissions Management (CRUD)
  - User Access Management (CRUD)

### **Frontend:**
- [ ] ربط الصفحات بـ tRPC queries
- [ ] إضافة وظائف CRUD للشاشات
- [ ] إضافة وظائف CRUD للصلاحيات
- [ ] إضافة وظائف منح/سحب الوصول
- [ ] إضافة فلاتر وبحث

### **التكامل:**
- [ ] ربط الصلاحيات بنظام الصلاحيات الموجود
- [ ] ربط الشاشات بالوظائف الموجودة في النظام
- [ ] إضافة API endpoints للتطبيقات

---

## 🎯 الفائدة

1. **التحكم المركزي:** إدارة جميع شاشات ووظائف التطبيقات من مكان واحد
2. **المرونة:** إضافة/تعديل/حذف شاشات ووظائف بسهولة
3. **الأمان:** نظام صلاحيات متقدم للتحكم بالوصول
4. **التتبع:** تتبع وصول المستخدمين وآخر نشاط
5. **التخصيص:** تخصيص التطبيقات حسب احتياجات كل عمل

---

**تاريخ الإنشاء:** 2024
**الحالة:** ✅ الأساسيات مكتملة - يحتاج Backend Router + Database Migration

