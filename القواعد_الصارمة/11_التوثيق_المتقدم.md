# 🟠 قواعد التوثيق والصيانة المتقدمة (Documentation & Maintenance)

> **الفئة:** التوثيق والصيانة المتقدمة  
> **عدد القواعد:** 3 قواعد  
> **مستوى الأهمية:** متوسط

---

## القاعدة 33: توثيق الـ API (API Documentation Rule)

> **كل endpoint يجب أن يكون موثقاً بالكامل: المسار، الطريقة، المدخلات، المخرجات، الأخطاء.**

**هيكل التوثيق:**
```typescript
/**
 * @api {post} /api/users إنشاء مستخدم جديد
 * @apiName CreateUser
 * @apiGroup Users
 * @apiVersion 1.0.0
 * 
 * @apiHeader {String} Authorization Bearer token
 * 
 * @apiBody {String} name اسم المستخدم (2-100 حرف)
 * @apiBody {String} email البريد الإلكتروني
 * @apiBody {String} password كلمة المرور (8+ حروف)
 * 
 * @apiSuccess {Object} user بيانات المستخدم
 * @apiSuccess {Number} user.id معرف المستخدم
 * @apiSuccess {String} user.name اسم المستخدم
 * @apiSuccess {String} user.email البريد الإلكتروني
 * 
 * @apiError (400) VAL_INVALID_EMAIL البريد الإلكتروني غير صالح
 * @apiError (409) RES_ALREADY_EXISTS البريد مستخدم مسبقاً
 * @apiError (401) AUTH_UNAUTHORIZED غير مصرح
 * 
 * @apiExample {curl} مثال:
 *     curl -X POST https://api.example.com/users \
 *       -H "Authorization: Bearer xxx" \
 *       -d '{"name":"أحمد","email":"ahmed@example.com","password":"Pass123!"}'
 */
```

**العقوبة:** رفض الدمج - أي endpoint جديد بدون توثيق يُرفض.

---

## القاعدة 34: التعليقات الذكية (Smart Comments Rule)

> **التعليقات يجب أن تشرح "لماذا" وليس "ماذا".**

**أمثلة:**
```typescript
// ❌ خطأ - تعليق يشرح "ماذا" (واضح من الكود)
// زيادة العداد بواحد
counter++;

// ✅ صحيح - تعليق يشرح "لماذا"
// نبدأ من 1 وليس 0 لأن الـ API الخارجي يتوقع ترقيم يبدأ من 1
counter++;

// ❌ خطأ - تعليق غير مفيد
// حلقة for
for (let i = 0; i < items.length; i++) { }

// ✅ صحيح - تعليق يشرح السبب
// نستخدم for بدلاً من forEach للتمكن من الخروج مبكراً عند إيجاد العنصر
for (let i = 0; i < items.length; i++) {
  if (items[i].id === targetId) break;
}

// ✅ ممتاز - تعليق يشرح قرار تصميمي
/**
 * نستخدم debounce بـ 300ms لأن:
 * 1. المستخدم يكتب بمعدل 200ms بين الأحرف
 * 2. نريد تقليل الطلبات للـ API
 * 3. 300ms توازن جيد بين الاستجابة وتقليل الحمل
 */
const debouncedSearch = useMemo(
  () => debounce(handleSearch, 300),
  [handleSearch]
);
```

**العقوبة:** تحذير - التعليقات غير المفيدة تُطلب إزالتها أو تحسينها.

---

## القاعدة 35: النسخ الاحتياطي (Backup Strategy Rule)

> **يجب وجود خطة واضحة للنسخ الاحتياطي واستعادة البيانات.**

**خطة النسخ الاحتياطي:**

| النوع | التكرار | الاحتفاظ | المكان |
|:---|:---:|:---:|:---|
| **Full Backup** | يومياً | 30 يوم | Cloud Storage |
| **Incremental** | كل 6 ساعات | 7 أيام | Cloud Storage |
| **Transaction Log** | كل ساعة | 24 ساعة | Local + Cloud |

**قائمة التحقق:**
- [ ] النسخ الاحتياطي يعمل تلقائياً
- [ ] تم اختبار الاستعادة (Restore Test)
- [ ] النسخ مشفرة
- [ ] النسخ في موقع جغرافي مختلف
- [ ] تنبيهات عند فشل النسخ

**العقوبة:** تحذير حرج - النظام بدون خطة backup يُوقف حتى الإصلاح.

---

## ملخص قواعد التوثيق المتقدم

| # | القاعدة | العقوبة |
|:---:|:---|:---:|
| 33 | توثيق الـ API الكامل | رفض |
| 34 | التعليقات الذكية (لماذا وليس ماذا) | تحذير |
| 35 | خطة النسخ الاحتياطي | تحذير حرج |
