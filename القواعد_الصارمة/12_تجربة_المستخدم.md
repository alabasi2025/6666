# 🔷 قواعد تجربة المستخدم (User Experience)

> **الفئة:** تجربة المستخدم  
> **عدد القواعد:** 3 قواعد  
> **مستوى الأهمية:** متوسط

---

## القاعدة 36: التغذية الراجعة الفورية (Instant Feedback Rule)

> **كل إجراء من المستخدم يجب أن يحصل على تغذية راجعة فورية.**

**أنواع التغذية الراجعة:**

| الإجراء | التغذية الراجعة |
|:---|:---|
| نقر زر | تغيير لون + loading |
| إرسال نموذج | رسالة نجاح/خطأ |
| تحميل بيانات | skeleton أو spinner |
| حذف عنصر | تأكيد + رسالة نجاح |
| خطأ في الإدخال | رسالة تحت الحقل فوراً |

**مثال:**
```typescript
function SubmitButton({ isLoading, onClick }) {
  return (
    <button 
      onClick={onClick}
      disabled={isLoading}
      className={isLoading ? 'opacity-50' : ''}
    >
      {isLoading ? (
        <>
          <Spinner className="animate-spin" />
          جارٍ الحفظ...
        </>
      ) : (
        'حفظ'
      )}
    </button>
  );
}
```

**العقوبة:** تحذير - أي إجراء بدون feedback يُطلب تحسينه.

---

## القاعدة 37: رسائل الخطأ الودية (Friendly Error Messages Rule)

> **رسائل الخطأ يجب أن تكون واضحة، ودية، وتقدم حلاً.**

**هيكل الرسالة الجيدة:**
```
[ماذا حدث] + [لماذا حدث (اختياري)] + [ماذا تفعل]
```

**أمثلة:**
```typescript
// ❌ خطأ - رسائل غير مفيدة
"Error 500"
"Invalid input"
"Something went wrong"

// ✅ صحيح - رسائل ودية ومفيدة
"لم نتمكن من حفظ البيانات. يرجى التحقق من اتصالك بالإنترنت والمحاولة مرة أخرى."
"البريد الإلكتروني غير صالح. يرجى إدخال بريد بصيغة example@domain.com"
"كلمة المرور ضعيفة. يجب أن تحتوي على 8 أحرف على الأقل، حرف كبير، ورقم."
"هذا البريد مسجل مسبقاً. هل تريد تسجيل الدخول بدلاً من ذلك؟"
```

**العقوبة:** تحذير - الرسائل غير الودية تُطلب تحسينها.

---

## القاعدة 38: إمكانية الوصول (Accessibility Rule)

> **النظام يجب أن يكون قابلاً للاستخدام من الجميع، بما في ذلك ذوي الاحتياجات الخاصة.**

**متطلبات إمكانية الوصول:**

| المتطلب | التطبيق |
|:---|:---|
| **تباين الألوان** | نسبة 4.5:1 على الأقل |
| **حجم الخط** | 16px كحد أدنى |
| **التنقل بلوحة المفاتيح** | Tab للتنقل، Enter للتفعيل |
| **قارئ الشاشة** | aria-label لكل عنصر تفاعلي |
| **النصوص البديلة** | alt لكل صورة |

**مثال:**
```tsx
// ✅ صحيح - زر قابل للوصول
<button
  aria-label="حذف المستخدم أحمد"
  onClick={handleDelete}
  onKeyDown={(e) => e.key === 'Enter' && handleDelete()}
  tabIndex={0}
>
  <TrashIcon aria-hidden="true" />
  <span className="sr-only">حذف</span>
</button>

// ✅ صحيح - نموذج قابل للوصول
<label htmlFor="email">البريد الإلكتروني</label>
<input
  id="email"
  type="email"
  aria-required="true"
  aria-invalid={!!errors.email}
  aria-describedby="email-error"
/>
{errors.email && (
  <span id="email-error" role="alert">
    {errors.email}
  </span>
)}
```

**العقوبة:** تحذير - المكونات غير القابلة للوصول تُطلب تحسينها.

---

## ملخص قواعد تجربة المستخدم

| # | القاعدة | العقوبة |
|:---:|:---|:---:|
| 36 | التغذية الراجعة الفورية | تحذير |
| 37 | رسائل الخطأ الودية | تحذير |
| 38 | إمكانية الوصول (Accessibility) | تحذير |
