# 🔍 فحص حالة السيرفر - Server Status Check

**تاريخ:** 6 يناير 2026

---

## ⚠️ **ملاحظة مهمة**

السيرفر يحتاج تشغيلاً يدوياً في terminal منفصل. لا يمكن تشغيله تلقائياً من خلال الأدوات المتاحة.

---

## 🚀 **الطريقة الصحيحة للتشغيل:**

### **1. افتح PowerShell جديد:**

اضغط `Win + X` واختر **"Windows PowerShell"** أو **"Terminal"**

### **2. نفذ الأوامر التالية:**

```powershell
# الانتقال للمجلد
cd F:\666666\6666-main

# تعيين المتغيرات البيئية
$env:PORT='8000'
$env:NODE_ENV='development'

# تشغيل السيرفر
npm run dev
```

### **3. انتظر حتى ترى:**

```
✓ Server started on port 8000
✓ Vite server ready
```

عادة يستغرق 10-30 ثانية.

---

## ✅ **التحقق من التشغيل:**

### **في Terminal:**
- يجب أن ترى رسائل بدء السيرفر
- لا يجب أن ترى أخطاء

### **في المتصفح:**
- افتح: `http://localhost:8000`
- يجب أن ترى صفحة تسجيل الدخول أو Dashboard

---

## 🐛 **استكشاف الأخطاء:**

### **1. خطأ: "Cannot find module"**
```powershell
npm install
```

### **2. خطأ: "Port 8000 is already in use"**
```powershell
# إيقاف جميع عمليات Node.js
Get-Process | Where-Object {$_.ProcessName -eq "node"} | Stop-Process -Force
```

### **3. خطأ: "Database connection failed"**
- تأكد من وجود `.env` مع `DEMO_MODE=true`
- أو تأكد من إعدادات قاعدة البيانات

---

## 📋 **بعد التشغيل الناجح:**

1. ✅ افتح `http://localhost:8000`
2. ✅ سجل الدخول
3. ✅ اذهب إلى: **المحركات الأساسية**
4. ✅ اختبر الشاشات:
   - محرك التسعير
   - محرك التسوية
   - محرك الجدولة
   - محرك الإسناد
   - فحص الصحة

---

**ملاحظة:** السيرفر يجب أن يبقى قيد التشغيل في Terminal. لا تغلق Terminal أثناء الاستخدام.

