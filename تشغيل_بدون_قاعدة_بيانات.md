# تشغيل النظام بدون قاعدة بيانات

## ✅ ما تم إنجازه

1. ✅ تم استنساخ المشروع من GitHub
2. ✅ تم إنشاء ملف `.env` مع الإعدادات الأساسية
3. ✅ تم تفعيل **DEMO_MODE=true** في ملف `.env`

## 📋 المتطلبات

قبل تشغيل النظام، يجب تثبيت:

### 1. Node.js (مطلوب)
- **الرابط**: https://nodejs.org/
- **الإصدار المطلوب**: v18 أو أحدث (يفضل v20+)
- **كيفية التثبيت**: 
  - حمّل المثبت من الموقع الرسمي
  - شغّل المثبت واتبع التعليمات
  - تأكد من اختيار "Add to PATH" أثناء التثبيت

### 2. pnpm (مطلوب)
بعد تثبيت Node.js، افتح PowerShell وثبت pnpm:

```powershell
npm install -g pnpm
```

للتحقق من التثبيت:
```powershell
pnpm --version
```

## 🚀 خطوات التشغيل (بدون قاعدة بيانات)

### الخطوة 1: التأكد من إعدادات DEMO_MODE

افتح ملف `.env` وتأكد من وجود السطر التالي:

```env
DEMO_MODE=true
```

إذا لم يكن موجوداً، أضفه في نهاية الملف.

### الخطوة 2: تثبيت الاعتماديات

افتح PowerShell في مجلد المشروع (`F:\666666\6666-main`) وقم بتنفيذ:

```powershell
pnpm install
```

هذه العملية قد تستغرق عدة دقائق (5-10 دقائق).

### الخطوة 3: تشغيل النظام

```powershell
pnpm dev
```

بعد التشغيل، يجب أن ترى رسائل مثل:

```
[Database] DATABASE_URL not set, running in DEMO_MODE
[Database] Failed to connect
Server running on http://0.0.0.0:5000/
Vite server setup complete
```

**ملاحظة مهمة**: 
- ⚠️ قد ترى تحذيرات حول فشل الاتصال بقاعدة البيانات - هذا طبيعي في وضع DEMO_MODE
- ✅ النظام سيعمل بشكل جزئي (الواجهة الأمامية + بعض الوظائف)
- ✅ يمكنك تصفح الواجهة والتنقل بين الصفحات
- ❌ الوظائف التي تحتاج قاعدة بيانات لن تعمل (مثل حفظ البيانات، تسجيل الدخول الحقيقي)

### الخطوة 4: فتح المتصفح

افتح المتصفح وانتقل إلى:
```
http://localhost:5000
```

## 🎯 ما سيعمل في وضع DEMO_MODE

✅ **سيعمل:**
- الواجهة الأمامية (Frontend)
- التنقل بين الصفحات
- عرض الواجهات والتصاميم
- Vite Development Server
- Hot Module Replacement (HMR)

❌ **لن يعمل:**
- تسجيل الدخول الحقيقي (سيستخدم مستخدم تجريبي)
- حفظ البيانات في قاعدة البيانات
- استرجاع البيانات من قاعدة البيانات
- العمليات التي تحتاج قاعدة بيانات

## 🔐 المستخدم التجريبي

في وضع DEMO_MODE، سيتم استخدام مستخدم تجريبي تلقائياً:

- **الاسم**: مستخدم تجريبي
- **الدور**: super_admin
- **الصلاحيات**: جميع الصلاحيات

## 🛠️ استكشاف الأخطاء

### المشكلة: `pnpm: command not found`
**الحل**: تأكد من تثبيت Node.js ثم قم بتثبيت pnpm:
```powershell
npm install -g pnpm
```

### المشكلة: `node: command not found`
**الحل**: 
1. تأكد من تثبيت Node.js
2. أعد تشغيل PowerShell بعد التثبيت
3. تحقق من PATH:
```powershell
$env:PATH -split ';' | Select-String node
```

### المشكلة: `Port 5000 is already in use`
**الحل**: 
- النظام سيحاول استخدام منفذ آخر تلقائياً (5001, 5002, ...)
- أو غيّر المنفذ في ملف `.env`:
```env
PORT=3000
```

### المشكلة: `Cannot find module`
**الحل**: 
- تأكد من تثبيت جميع الاعتماديات:
```powershell
pnpm install
```

### المشكلة: `Vite server failed to start`
**الحل**: 
- تحقق من أن المنفذ غير مستخدم
- راجع ملف `vite.config.ts` للإعدادات

## 📝 ملاحظات مهمة

1. **وضع DEMO_MODE**: هذا الوضع مخصص للاختبار والتطوير فقط
2. **لا توجد بيانات حقيقية**: جميع البيانات ستكون تجريبية
3. **الأمان**: لا تستخدم DEMO_MODE في الإنتاج
4. **قاعدة البيانات**: عندما تكون جاهزة، أزل `DEMO_MODE=true` من `.env` وأضف `DATABASE_URL`

## 🔄 الانتقال من DEMO_MODE إلى قاعدة البيانات

عندما تكون قاعدة البيانات جاهزة:

1. **أزل** `DEMO_MODE=true` من ملف `.env`
2. **أضف** `DATABASE_URL` مع بيانات الاتصال:
```env
DATABASE_URL=mysql://root:YOUR_PASSWORD@localhost:3306/energy_management
```

3. **أنشئ قاعدة البيانات**:
```sql
CREATE DATABASE IF NOT EXISTS energy_management;
```

4. **شغّل Migration**:
```powershell
pnpm db:push
```

5. **أعد تشغيل الخادم**:
```powershell
pnpm dev
```

## 📚 ملفات مفيدة

- `SYSTEM_STARTUP_REPORT.md` - تقرير شامل عن النظام
- `USER_DEPLOYMENT_GUIDE.md` - دليل النشر للمستخدم
- `DEPLOYMENT_INSTRUCTIONS.md` - تعليمات النشر التفصيلية
- `تعليمات_الإعداد.md` - دليل الإعداد الكامل

## 🎉 الخطوات التالية

بعد تشغيل النظام بنجاح:

1. ✅ تصفح الواجهة الأمامية
2. ✅ اختبر التنقل بين الصفحات
3. ✅ راجع التصاميم والواجهات
4. ⏳ جهز قاعدة البيانات MySQL
5. ⏳ انتقل من DEMO_MODE إلى قاعدة البيانات الحقيقية

---

**تاريخ الإنشاء**: 2 يناير 2026  
**الإصدار**: 2.2.0  
**الوضع**: DEMO_MODE (بدون قاعدة بيانات)

