# 🚀 دليل البدء السريع - Quick Start Guide

**تاريخ الإنشاء:** 6 يناير 2026  
**الحالة:** ✅ جاهز للاستخدام

---

## 📋 **الخطوات الأولى**

### **1. تثبيت Dependencies (إن لزم)**

```bash
# إذا كنت تستخدم node-cron (اختياري)
npm install node-cron @types/node-cron
```

### **2. تشغيل Migration**

```bash
# تشغيل migration لجدول pricing_rules
# الملف: drizzle/migrations/0006_add_pricing_rules.sql
npm run db:push
```

أو تشغيل SQL مباشرة:

```sql
-- انظر: drizzle/migrations/0006_add_pricing_rules.sql
```

### **3. التحقق من الحسابات المحاسبية**

تأكد من وجود الحسابات التالية في شجرة الحسابات:

| الكود | الاسم | النوع |
|-------|-------|-------|
| 1100 | النقدية | أصل (مدين) |
| 1110 | البنك | أصل (مدين) |
| 1200 | العملاء | أصل (مدين) |
| 1300 | المخزون | أصل (مدين) |
| 2100 | الموردين | التزام (دائن) |
| 4100 | إيرادات الكهرباء | إيراد (دائن) |
| 4101 | إيراد دفع مسبق | إيراد (دائن) |
| 5100 | رواتب | مصروف (مدين) |

### **4. تهيئة Cron Jobs**

Cron Jobs يتم تهيئتها تلقائياً عند بدء الخادم. لا حاجة لإجراءات إضافية.

---

## 🎯 **الاستخدام السريع**

### **1. حساب سعر اشتراك**

```typescript
const pricing = await trpc.pricing.calculate.query({
  businessId: 1,
  meterType: "sts",
  usageType: "residential"
});
```

### **2. إنشاء فاتورة (مع قيد تلقائي)**

```typescript
const invoice = await trpc.invoice.create.mutate({
  businessId: 1,
  customerId: 123,
  totalAmount: 1000,
  // ... باقي البيانات
});
// ✅ القيد المحاسبي تم إنشاؤه تلقائياً!
```

### **3. إسناد مهمة طارئة**

```typescript
const assignment = await trpc.smartAssignment.assignEmergency.mutate({
  businessId: 1,
  operationId: 401,
  taskLatitude: 24.7136,
  taskLongitude: 46.6753,
  maxDistance: 20
});
```

---

## 📚 **المراجع**

- **مرجع API الكامل:** `COMPLETE_API_REFERENCE.md`
- **أمثلة الاستخدام:** `USAGE_EXAMPLES.md`
- **ملخص التنفيذ:** `FINAL_IMPLEMENTATION_SUMMARY.md`

---

## ⚠️ **ملاحظات مهمة**

1. **STS لا يحتاج تأمين:** محرك التسعير يتعامل مع هذا تلقائياً
2. **القيود التلقائية:** يتم إنشاؤها تلقائياً عند إنشاء الفواتير والمدفوعات
3. **الصيانة الوقائية:** تعمل تلقائياً كل ليلة (Cron Job)
4. **الإسناد الذكي:** يعمل تلقائياً عند إنشاء عمليات طارئة

---

## 🔧 **استكشاف الأخطاء**

### **مشكلة: لا يتم إنشاء القيود التلقائية**

**الحل:**
- تأكد من وجود الحسابات المحاسبية المطلوبة
- تحقق من سجلات الأخطاء (logs)
- تأكد من أن الفترة المحاسبية نشطة

### **مشكلة: لا يتم إسناد المهام الطارئة**

**الحل:**
- تأكد من وجود فنيين متاحين (status = "available")
- تحقق من وجود إحداثيات GPS للفنيين
- تأكد من أن المسافة القصوى كافية

### **مشكلة: Cron Jobs لا تعمل**

**الحل:**
- تحقق من أن الخادم يعمل
- تحقق من سجلات الأخطاء
- تأكد من تهيئة CronJobsManager في `server/_core/index.ts`

---

## ✅ **التحقق من التثبيت**

```typescript
// 1. التحقق من محرك التسعير
const pricing = await trpc.pricing.calculate.query({
  businessId: 1,
  meterType: "sts",
  usageType: "residential"
});
// يجب أن يعيد: { subscriptionFee: 7000, depositAmount: 0, ... }

// 2. التحقق من Cron Jobs
// تحقق من logs عند بدء الخادم:
// "Cron Jobs initialized: 15 jobs scheduled"

// 3. التحقق من الربط
// أنشئ فاتورة وتحقق من وجود قيد محاسبي تلقائي
```

---

## 🎉 **جاهز للاستخدام!**

جميع المحركات جاهزة ومربوطة. ابدأ باستخدام API endpoints!

---

**آخر تحديث:** 6 يناير 2026

