# 🌱 دليل البيانات الافتراضية (Seed Data)

## 📋 المحتوى

هذا الملف يشرح كيفية إنشاء البيانات الافتراضية عند تثبيت النظام لأول مرة.

---

## 🎯 ما يتم إنشاؤه

### 1. المستخدم الافتراضي (Admin)

```
📱 رقم الهاتف: 0500000000
🔑 كلمة المرور: admin123
👤 الدور: super_admin
📧 البريد: admin@system.local
```

### 2. الأدوار الافتراضية (7 أدوار)

| الدور | الاسم | الوصف |
|-------|-------|-------|
| `super_admin` | مدير النظام | صلاحيات كاملة |
| `admin` | مدير | صلاحيات إدارية |
| `manager` | مدير محطة | إدارة محطة واحدة |
| `accountant` | محاسب | العمليات المالية |
| `technician` | فني | العمليات الفنية |
| `collector` | محصل | تحصيل الفواتير |
| `user` | مستخدم | صلاحيات أساسية |

### 3. الصلاحيات الافتراضية (30+ صلاحية)

مقسمة على الوحدات:
- **Core:** إدارة المستخدمين، الأدوار، الإعدادات
- **Billing:** الفواتير، المدفوعات
- **Customers:** إدارة العملاء
- **Operations:** أوامر العمل
- **Inventory:** المخزون
- **Finance:** الحسابات المالية
- **Reports:** التقارير

---

## 🚀 طرق التشغيل

### الطريقة 1: عبر NPM Script

```bash
cd /home/ubuntu/6666
pnpm seed
```

### الطريقة 2: عبر السكريبت المباشر

```bash
cd /home/ubuntu/6666
./scripts/seed.sh
```

### الطريقة 3: عبر Node مباشرة

```bash
cd /home/ubuntu/6666
pnpm tsx server/seed.ts
```

---

## ⚙️ التشغيل التلقائي

### إضافة Seed إلى package.json

```json
{
  "scripts": {
    "seed": "tsx server/seed.ts",
    "dev": "concurrently \"pnpm dev:server\" \"pnpm dev:client\"",
    "dev:server": "tsx watch server/index.ts",
    "postinstall": "pnpm seed"
  }
}
```

### التشغيل عند بدء السيرفر

في ملف `server/index.ts`:

```typescript
import { runSeed } from './seed';

// بعد الاتصال بقاعدة البيانات
async function startServer() {
  // ...
  
  // تشغيل seed
  await runSeed();
  
  // بدء السيرفر
  app.listen(PORT, () => {
    console.log(`Server running on port ${PORT}`);
  });
}
```

---

## 🔒 الأمان

### تغيير كلمة المرور الافتراضية

**⚠️ مهم جداً:** يجب تغيير كلمة مرور المدير فوراً بعد أول تسجيل دخول!

```typescript
// في server/seed.ts
const DEFAULT_DATA = {
  admin: {
    phone: '0500000000',
    password: 'كلمة_مرور_قوية_هنا', // غير هذا!
    // ...
  }
};
```

### تعطيل Seed في الإنتاج

```typescript
// في server/index.ts
if (process.env.NODE_ENV !== 'production') {
  await runSeed();
}
```

---

## 🧪 الاختبار

### 1. تشغيل Seed

```bash
pnpm seed
```

### 2. التحقق من النتيجة

يجب أن ترى:

```
===========================================
✅ تم إنشاء المستخدم الافتراضي بنجاح!
===========================================
📱 رقم الهاتف: 0500000000
🔑 كلمة المرور: admin123
👤 الدور: super_admin
===========================================
```

### 3. تسجيل الدخول

```bash
# افتح المتصفح
http://localhost:5173/login

# أدخل:
رقم الهاتف: 0500000000
كلمة المرور: admin123
```

---

## 🔄 إعادة Seed

إذا كنت تريد إعادة إنشاء البيانات:

```bash
# 1. حذف البيانات الموجودة
pnpm db:reset

# 2. تشغيل Seed مرة أخرى
pnpm seed
```

---

## 📝 تخصيص البيانات

### إضافة مستخدمين إضافيين

في `server/seed.ts`:

```typescript
const DEFAULT_DATA = {
  // ...
  additionalUsers: [
    {
      phone: '0501111111',
      password: 'user123',
      name: 'مدير المحطة 1',
      role: 'manager'
    },
    {
      phone: '0502222222',
      password: 'user123',
      name: 'محاسب',
      role: 'accountant'
    }
  ]
};
```

### إضافة صلاحيات جديدة

```typescript
permissions: [
  // ...
  {
    module: 'custom',
    name: 'my_permission',
    displayName: 'صلاحيتي الخاصة',
    description: 'وصف الصلاحية'
  }
]
```

---

## ❓ الأسئلة الشائعة

### Q: هل يتم تشغيل Seed في كل مرة؟

**A:** لا، الكود يتحقق أولاً من وجود بيانات. إذا وجدت بيانات، يتم تخطي Seed.

### Q: ماذا لو نسيت كلمة المرور؟

**A:** يمكنك:
1. إعادة تشغيل Seed بعد حذف المستخدم
2. أو استخدام أداة إعادة تعيين كلمة المرور

### Q: كيف أضيف بيانات تجريبية؟

**A:** أنشئ ملف `server/seed-demo.ts` منفصل للبيانات التجريبية.

---

## 🐛 استكشاف الأخطاء

### خطأ: "قاعدة البيانات غير متاحة"

```bash
# تأكد من تشغيل قاعدة البيانات
docker-compose up -d

# تأكد من DATABASE_URL في .env
DATABASE_URL=mysql://user:password@localhost:3306/dbname
```

### خطأ: "رقم الهاتف مسجل مسبقاً"

```bash
# البيانات موجودة مسبقاً، لا داعي للقلق
# أو احذف المستخدم وأعد المحاولة
```

---

## 📞 الدعم

إذا واجهت أي مشاكل:
1. تحقق من logs: `tail -f logs/app.log`
2. تحقق من قاعدة البيانات: `pnpm db:studio`
3. راجع التوثيق: `docs/`

---

**✅ الآن النظام جاهز للاستخدام مع البيانات الافتراضية!**
