# القواعد الصارمة للتطوير والصيانة

> **تاريخ الإنشاء:** 24 ديسمبر 2025  
> **آخر تحديث:** 24 ديسمبر 2025  
> **الإصدار:** 1.0.0

---

## مقدمة

هذه الوثيقة تحدد القواعد الصارمة التي يجب الالتزام بها من قبل جميع المطورين والمساهمين في هذا المشروع. تم وضع هذه القواعد بناءً على فحص شامل للمستودع وتحليل الفجوات والمشاكل المكتشفة. **الالتزام بهذه القواعد إلزامي وليس اختيارياً.**

---

## 🔴 قواعد الأمان (Security Rules)

### القاعدة 1: الأسرار الصفرية في الكود

> **لا يُسمح بوجود أي كلمة مرور أو مفتاح API أو سر في الكود المصدري مطلقاً.**

**التطبيق:**
- جميع الأسرار يجب أن تكون في متغيرات البيئة (`.env`).
- يجب رفض أي Pull Request يحتوي على أسرار ثابتة.
- استخدام أدوات مثل `git-secrets` أو `gitleaks` للفحص التلقائي.

**أمثلة محظورة:**
```typescript
// ❌ محظور
const password = "admin123";
const apiKey = "sk-1234567890";

// ✅ صحيح
const password = process.env.ADMIN_PASSWORD;
const apiKey = process.env.API_KEY;
```

**العقوبة:** رفض الدمج فوراً.

---

### القاعدة 2: الملفات المحظورة

> **لا يُسمح برفع ملفات قواعد البيانات أو النسخ الاحتياطية أو البيانات الحساسة للمستودع.**

**الملفات المحظورة:**
- `*.sql` (ملفات النسخ الاحتياطي)
- `*.backup`, `*.dump`
- `*.env` (ملفات البيئة الفعلية)
- `*.pem`, `*.key` (المفاتيح الخاصة)

**التطبيق:**
- إضافة هذه الامتدادات إلى `.gitignore`.
- فحص المستودع دورياً للتأكد من عدم وجود ملفات حساسة.

**العقوبة:** رفض الدمج فوراً وحذف الملف من تاريخ Git.

---

### القاعدة 3: الحماية الإلزامية

> **كل نقطة نهاية (endpoint) حساسة يجب أن تكون محمية بـ Rate Limiting.**

**الحدود المطلوبة:**

| نوع النقطة | الحد الأقصى | الفترة الزمنية |
|:---|:---:|:---:|
| تسجيل الدخول | 10 محاولات | 15 دقيقة |
| التسجيل | 5 محاولات | 15 دقيقة |
| API العامة | 100 طلب | دقيقة واحدة |
| تحميل الملفات | 10 طلبات | دقيقة واحدة |

**المتطلبات الإضافية:**
- استخدام `helmet` لجميع رؤوس الأمان.
- تفعيل CORS بشكل صحيح.
- استخدام HTTPS في الإنتاج.

**العقوبة:** رفض الدمج.

---

## 🔵 قواعد البناء والجودة (Build & Quality Rules)

### القاعدة 4: صفر أخطاء TypeScript

> **لا يُسمح بدمج أي كود يحتوي على أخطاء TypeScript أو يستخدم `@ts-nocheck`.**

**التطبيق:**
- يجب أن يمر `tsc --noEmit` بنجاح قبل أي دمج.
- `@ts-nocheck` و `@ts-ignore` محظورة إلا بموافقة مسبقة موثقة.
- إضافة فحص TypeScript إلى CI/CD pipeline.

**الاستثناءات المسموحة (بموافقة):**
- ملفات التكوين الخارجية.
- مكتبات الطرف الثالث بدون أنواع.

**العقوبة:** رفض الدمج.

---

### القاعدة 5: الاختبارات الإلزامية

> **لا يُسمح بدمج أي كود إذا كانت الاختبارات تفشل.**

**المتطلبات:**
- نسبة نجاح الاختبارات: **100%**.
- كل ميزة جديدة يجب أن تأتي مع اختبارات.
- الحد الأدنى لتغطية الكود: **70%**.

**أنواع الاختبارات المطلوبة:**
- اختبارات الوحدة (Unit Tests) للدوال.
- اختبارات التكامل (Integration Tests) للـ API.
- اختبارات E2E للميزات الحرجة.

**العقوبة:** رفض الدمج.

---

### القاعدة 6: حجم الملف

> **لا يُسمح بوجود ملف يتجاوز 500 سطر من الكود.**

**التطبيق:**
- الملفات الكبيرة يجب تقسيمها إلى وحدات أصغر.
- استخدام نمط التصميم المناسب (مثل Repository Pattern).

**الملفات التي تحتاج تقسيم حالياً:**
- `server/db.ts` (4800+ سطر)
- `drizzle/schema.ts` (3200+ سطر)
- `client/src/pages/Dashboard.tsx` (2000+ سطر)

**العقوبة:** تحذير في أول مرة، رفض الدمج في المرات التالية.

---

## 🟢 قواعد الأداء (Performance Rules)

### القاعدة 7: منع N+1 Query

> **يُحظر تماماً تنفيذ استعلامات قاعدة البيانات داخل حلقات `for` أو `forEach` أو `map`.**

**مثال محظور:**
```typescript
// ❌ محظور - N+1 Query
for (const user of users) {
  const orders = await db.select().from(orders).where(eq(orders.userId, user.id));
}
```

**الحل الصحيح:**
```typescript
// ✅ صحيح - استعلام واحد
const userIds = users.map(u => u.id);
const allOrders = await db.select().from(orders).where(inArray(orders.userId, userIds));
```

**العقوبة:** رفض الدمج.

---

### القاعدة 8: تحديد الأعمدة

> **يُحظر استخدام `select()` بدون تحديد الأعمدة المطلوبة.**

**مثال محظور:**
```typescript
// ❌ محظور - يجلب جميع الأعمدة
await db.select().from(users).where(...);
```

**الحل الصحيح:**
```typescript
// ✅ صحيح - يجلب الأعمدة المطلوبة فقط
await db.select({ 
  id: users.id, 
  name: users.nameAr,
  phone: users.phone 
}).from(users).where(...);
```

**الاستثناءات:**
- عند الحاجة الفعلية لجميع الأعمدة (مع توثيق السبب).

**العقوبة:** تحذير في أول مرة، رفض الدمج في المرات التالية.

---

### القاعدة 9: الفهرسة الإلزامية

> **كل عمود يُستخدم في `WHERE` أو `JOIN` أو `ORDER BY` يجب أن يكون له فهرس.**

**التطبيق:**
- تعريف الفهارس في `schema.ts` وليس في ملفات SQL منفصلة.
- مراجعة خطة الاستعلام (Query Plan) للاستعلامات البطيئة.

**مثال:**
```typescript
export const users = mysqlTable("users", {
  id: int("id").primaryKey().autoincrement(),
  phone: varchar("phone", { length: 20 }).notNull(),
  businessId: int("business_id"),
}, (table) => ({
  phoneIdx: index("phone_idx").on(table.phone),
  businessIdx: index("business_idx").on(table.businessId),
}));
```

**العقوبة:** تحذير.

---

### القاعدة 10: تحسين React

> **كل مكون يُعاد تصييره أكثر من 3 مرات في الثانية يجب تحسينه.**

**الأدوات المطلوبة:**
- `React.memo` للمكونات المستقرة.
- `useCallback` للدوال الممررة كـ props.
- `useMemo` للحسابات المكلفة.

**مثال:**
```typescript
// ✅ صحيح
export const DataTable = React.memo(({ data, columns }) => {
  const sortedData = useMemo(() => sortData(data), [data]);
  const handleClick = useCallback((id) => { ... }, []);
  
  return <Table data={sortedData} onClick={handleClick} />;
});
```

**العقوبة:** تحذير.

---

## 🟡 قواعد التوثيق والصيانة (Documentation & Maintenance Rules)
## 🟣 قواعد التطوير المتكامل (Full-Stack Development Rules)

### القاعدة 14: الربط الكامل عند إنشاء الواجهات (Full Integration Rule)

### القاعدة 15: البحث والتحقق قبل البناء (Search Before Build Rule)

> **يُحظر تماماً إنشاء أو بناء أي ميزة أو مكون أو شاشة جديدة قبل البحث والتحقق من أنها غير موجودة مسبقاً في النظام.**

هذه القاعدة تمنع **التكرار والازدواجية** في الكود وتضمن الاستفادة من الميزات الموجودة بدلاً من إعادة اختراع العجلة.

**المبدأ الأساسي:**
> "لا تبني شيئاً جديداً إلا بعد التأكد من عدم وجوده. وإن وُجد، قم بتعديله أو تحسينه بدلاً من تكراره."

**خطوات التحقق الإلزامية قبل البناء:**

#### 1. البحث في الكود المصدري (Code Search)
```bash
# البحث عن الميزة أو المكون في الكود
grep -rn "اسم_الميزة" --include="*.ts" --include="*.tsx"
grep -rn "FeatureName" --include="*.ts" --include="*.tsx"
```

#### 2. البحث في قاعدة البيانات (Database Search)
- [ ] التحقق من `drizzle/schema.ts` للجداول الموجودة
- [ ] التحقق من وجود جدول مشابه أو يخدم نفس الغرض

#### 3. البحث في الواجهات (UI Search)
- [ ] مراجعة `client/src/pages/` للشاشات الموجودة
- [ ] مراجعة `client/src/components/` للمكونات الموجودة

#### 4. البحث في الـ API (Backend Search)
- [ ] مراجعة `server/db.ts` للدوال الموجودة
- [ ] مراجعة `server/routers.ts` للـ Routes الموجودة

#### 5. مراجعة التوثيق (Documentation Review)
- [ ] مراجعة الكتاب المقدس للنظام
- [ ] مراجعة ملفات README و TODO

**قائمة التحقق الإلزامية:**

```markdown
## قائمة التحقق قبل بناء [اسم الميزة]

### البحث والتحقق
- [ ] بحثت في الكود المصدري ولم أجد ميزة مشابهة
- [ ] تحققت من schema.ts ولم أجد جدول مشابه
- [ ] راجعت الشاشات الموجودة ولم أجد شاشة تخدم نفس الغرض
- [ ] راجعت المكونات الموجودة ولم أجد مكون مشابه
- [ ] راجعت دوال الـ API ولم أجد دالة تؤدي نفس الوظيفة

### القرار
- [ ] ✅ الميزة غير موجودة → يمكن البناء من الصفر
- [ ] ⚠️ الميزة موجودة جزئياً → يجب التعديل والإضافة عليها
- [ ] ❌ الميزة موجودة بالكامل → لا حاجة للبناء
```

**السيناريوهات المحتملة:**

| نتيجة البحث | الإجراء المطلوب |
|:---|:---|
| الميزة **غير موجودة** | يمكن البناء من الصفر |
| الميزة **موجودة جزئياً** | التعديل والإضافة على الموجود |
| الميزة **موجودة لكن ناقصة** | إكمال النواقص فقط |
| الميزة **موجودة بالكامل** | عدم البناء - استخدام الموجود |
| يوجد **مكون مشابه** | إعادة استخدامه أو توسيعه |

**أمثلة عملية:**

```typescript
// ❌ خطأ: بناء دالة جديدة دون التحقق
async function getUsers() {
  // قد تكون موجودة بالفعل باسم آخر مثل fetchUsers أو listUsers
}

// ✅ صحيح: البحث أولاً
// 1. grep -rn "getUser\|fetchUser\|listUser" --include="*.ts"
// 2. إذا وجدت: استخدمها أو عدّلها
// 3. إذا لم توجد: أنشئها
```

**الأسئلة الإلزامية قبل البناء:**

1. **هل هذه الميزة موجودة بالفعل؟**
2. **هل يوجد شيء مشابه يمكن تعديله؟**
3. **هل يوجد مكون يمكن إعادة استخدامه؟**
4. **هل الجدول موجود في قاعدة البيانات؟**
5. **هل الـ API موجود ويحتاج فقط لتعديل؟**

**العقوبة:** رفض الدمج - لا يُقبل أي Pull Request لميزة جديدة بدون إثبات إجراء البحث والتحقق.

---

> **عند إنشاء أي واجهة أو شاشة جديدة، يجب إكمال الربط الكامل من الواجهة إلى قاعدة البيانات واختبار جميع العمليات الأساسية (CRUD).**

هذه القاعدة تضمن أن أي شاشة جديدة تكون **وظيفية بالكامل** وليست مجرد واجهة فارغة.

**المتطلبات الإلزامية:**

#### 1. إنشاء/التحقق من جدول قاعدة البيانات (Database Schema)
- [ ] التأكد من وجود الجدول في `drizzle/schema.ts`
- [ ] إضافة الجدول إذا كان غير موجود
- [ ] تشغيل `pnpm db:push` لتطبيق التغييرات

#### 2. إنشاء/التحقق من الـ Backend API
- [ ] إضافة الدوال في `server/db.ts`:
  - `create[Entity]` - للإضافة
  - `get[Entity]` / `get[Entities]` - للعرض
  - `update[Entity]` - للتعديل
  - `delete[Entity]` - للحذف
- [ ] إضافة الـ Routes في `server/routers.ts`:
  - `.mutation` للإضافة والتعديل والحذف
  - `.query` للعرض والقراءة

#### 3. ربط الواجهة الأمامية (Frontend Integration)
- [ ] استخدام `trpc.useQuery` لجلب البيانات
- [ ] استخدام `trpc.useMutation` للعمليات
- [ ] إضافة معالجة الأخطاء والتحميل

#### 4. اختبار العمليات الأربع (CRUD Testing)
- [ ] **إضافة (Create):** اختبار إضافة سجل جديد والتأكد من ظهوره
- [ ] **عرض (Read):** اختبار عرض البيانات بشكل صحيح
- [ ] **تعديل (Update):** اختبار تعديل سجل والتأكد من حفظ التغييرات
- [ ] **حذف (Delete):** اختبار حذف سجل والتأكد من إزالته

**قائمة التحقق الإلزامية (Checklist):**

```markdown
## قائمة التحقق لشاشة [اسم الشاشة]

### قاعدة البيانات
- [ ] الجدول موجود في schema.ts
- [ ] تم تشغيل db:push

### الباك إند
- [ ] دالة الإضافة (create)
- [ ] دالة العرض (get/list)
- [ ] دالة التعديل (update)
- [ ] دالة الحذف (delete)
- [ ] Routes مضافة في routers.ts

### الفرونت إند
- [ ] الشاشة مربوطة بـ useQuery
- [ ] الشاشة مربوطة بـ useMutation
- [ ] معالجة حالة التحميل
- [ ] معالجة حالة الخطأ

### الاختبار
- [ ] ✅ اختبار الإضافة - يعمل
- [ ] ✅ اختبار العرض - يعمل
- [ ] ✅ اختبار التعديل - يعمل
- [ ] ✅ اختبار الحذف - يعمل
```

**مثال على التطبيق الصحيح:**

```typescript
// 1. Schema (drizzle/schema.ts)
export const products = mysqlTable("products", {
  id: int("id").primaryKey().autoincrement(),
  name: varchar("name", { length: 255 }).notNull(),
  price: decimal("price", { precision: 10, scale: 2 }),
  createdAt: timestamp("created_at").defaultNow(),
});

// 2. Backend (server/db.ts)
export async function createProduct(data: InsertProduct) {
  const db = await getDb();
  return await db.insert(products).values(data);
}

export async function getProducts() {
  const db = await getDb();
  return await db.select().from(products);
}

export async function updateProduct(id: number, data: Partial<InsertProduct>) {
  const db = await getDb();
  return await db.update(products).set(data).where(eq(products.id, id));
}

export async function deleteProduct(id: number) {
  const db = await getDb();
  return await db.delete(products).where(eq(products.id, id));
}

// 3. Routes (server/routers.ts)
products: {
  list: publicProcedure.query(() => getProducts()),
  create: publicProcedure.input(insertProductSchema).mutation(({ input }) => createProduct(input)),
  update: publicProcedure.input(updateProductSchema).mutation(({ input }) => updateProduct(input.id, input)),
  delete: publicProcedure.input(z.number()).mutation(({ input }) => deleteProduct(input)),
}

// 4. Frontend (client/src/pages/Products.tsx)
const { data, isLoading } = trpc.products.list.useQuery();
const createMutation = trpc.products.create.useMutation();
const updateMutation = trpc.products.update.useMutation();
const deleteMutation = trpc.products.delete.useMutation();
```

**العقوبة:** رفض الدمج - لا يُقبل أي Pull Request لشاشة جديدة بدون إكمال جميع عناصر القائمة.

---

### القاعدة 11: التعليقات المحظورة

> **يُحظر ترك تعليقات `TODO` أو `FIXME` في الكود لأكثر من أسبوعين.**

**التطبيق:**
- كل `TODO` يجب أن يتحول إلى Issue في GitHub.
- مراجعة أسبوعية للتعليقات المعلقة.
- تحديد مسؤول وتاريخ لكل TODO.

**الصيغة المطلوبة:**
```typescript
// TODO(@username, 2025-01-01): وصف المهمة - Issue #123
```

**العقوبة:** تحذير.

---

### القاعدة 12: منع console.log

> **يُحظر وجود `console.log` في كود الإنتاج.**

**التطبيق:**
- استخدام نظام تسجيل (Logger) مثل `winston` أو `pino`.
- إضافة قاعدة ESLint لمنع `console.log`.

**البديل الصحيح:**
```typescript
// ❌ محظور
console.log("User logged in:", userId);

// ✅ صحيح
logger.info("User logged in", { userId });
```

**العقوبة:** تحذير في أول مرة، رفض الدمج في المرات التالية.

---

### القاعدة 13: التبعيات الآمنة

> **لا يُسمح بوجود ثغرات أمنية عالية أو حرجة في التبعيات.**

**جدول المعالجة:**

| مستوى الخطورة | المدة المسموحة للإصلاح |
|:---|:---:|
| حرجة (Critical) | فوراً |
| عالية (High) | 7 أيام |
| متوسطة (Moderate) | 30 يوماً |
| منخفضة (Low) | 90 يوماً |

**التطبيق:**
- تشغيل `pnpm audit` أسبوعياً.
- تحديث التبعيات التي تحتوي على ثغرات.

**العقوبة:** إلزامي حسب الجدول.

---

## 📋 ملخص القواعد والعقوبات

| # | القاعدة | الفئة | العقوبة |
|:---:|:---|:---|:---|
| 1 | لا أسرار في الكود | الأمان | رفض فوري |
| 2 | لا ملفات حساسة | الأمان | رفض فوري |
| 3 | Rate Limiting إلزامي | الأمان | رفض |
| 4 | صفر أخطاء TypeScript | الجودة | رفض |
| 5 | 100% نجاح الاختبارات | الجودة | رفض |
| 6 | حد 500 سطر للملف | الجودة | تحذير → رفض |
| 7 | منع N+1 Query | الأداء | رفض |
| 8 | تحديد أعمدة SELECT | الأداء | تحذير → رفض |
| 9 | فهرسة الأعمدة | الأداء | تحذير |
| 10 | تحسين React | الأداء | تحذير |
| 11 | حذف TODO القديمة | الصيانة | تحذير |
| 12 | منع console.log | الصيانة | تحذير → رفض |
| 13 | تحديث التبعيات | الصيانة | إلزامي |
| 14 | الربط الكامل للواجهات | التطوير المتكامل | رفض |
| 15 | البحث والتحقق قبل البناء | التطوير المتكامل | رفض |

---

## 🔄 منهجية الترحيل والتحقق

عند إتمام أي مهمة، يجب اتباع الخطوات التالية بشكل صارم:

1. **الكومنت (Commit):** القيام بعملية الكومنت للتغييرات.
2. **تحديث الإصدار (Update Version):** تحديث رقم الإصدار (الرقم الثالث - Patch).
3. **الرفع (Push):** رفع الكومنت إلى مستودع Git.
4. **النشر (Publishing):** نشر التغييرات على بيئة الاستضافة.
5. **التحقق من النشر (Browser Test):** فتح المتصفح والتحقق من الإضافات.
6. **اختبار الشاشات (Screen Test):** إجراء اختبار شامل للشاشات والإضافات الجديدة.

---

## 📝 التوقيع والالتزام

بالمساهمة في هذا المستودع، أنت توافق على الالتزام بجميع القواعد المذكورة أعلاه.

---

**تم إعداد هذه الوثيقة بواسطة:** Manus AI  
**بناءً على:** فحص شامل للمستودع وتحليل الفجوات
