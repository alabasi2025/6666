# القواعد الصارمة للتطوير والصيانة

> **تاريخ الإنشاء:** 24 ديسمبر 2025  
> **آخر تحديث:** 24 ديسمبر 2025  
> **الإصدار:** 1.0.0

---

## مقدمة

هذه الوثيقة تحدد القواعد الصارمة التي يجب الالتزام بها من قبل جميع المطورين والمساهمين في هذا المشروع. تم وضع هذه القواعد بناءً على فحص شامل للمستودع وتحليل الفجوات والمشاكل المكتشفة. **الالتزام بهذه القواعد إلزامي وليس اختيارياً.**

---

## 🔴 قواعد الأمان (Security Rules)

### القاعدة 1: الأسرار الصفرية في الكود

> **لا يُسمح بوجود أي كلمة مرور أو مفتاح API أو سر في الكود المصدري مطلقاً.**

**التطبيق:**
- جميع الأسرار يجب أن تكون في متغيرات البيئة (`.env`).
- يجب رفض أي Pull Request يحتوي على أسرار ثابتة.
- استخدام أدوات مثل `git-secrets` أو `gitleaks` للفحص التلقائي.

**أمثلة محظورة:**
```typescript
// ❌ محظور
const password = "admin123";
const apiKey = "sk-1234567890";

// ✅ صحيح
const password = process.env.ADMIN_PASSWORD;
const apiKey = process.env.API_KEY;
```

**العقوبة:** رفض الدمج فوراً.

---

### القاعدة 2: الملفات المحظورة

> **لا يُسمح برفع ملفات قواعد البيانات أو النسخ الاحتياطية أو البيانات الحساسة للمستودع.**

**الملفات المحظورة:**
- `*.sql` (ملفات النسخ الاحتياطي)
- `*.backup`, `*.dump`
- `*.env` (ملفات البيئة الفعلية)
- `*.pem`, `*.key` (المفاتيح الخاصة)

**التطبيق:**
- إضافة هذه الامتدادات إلى `.gitignore`.
- فحص المستودع دورياً للتأكد من عدم وجود ملفات حساسة.

**العقوبة:** رفض الدمج فوراً وحذف الملف من تاريخ Git.

---

### القاعدة 3: الحماية الإلزامية

> **كل نقطة نهاية (endpoint) حساسة يجب أن تكون محمية بـ Rate Limiting.**

**الحدود المطلوبة:**

| نوع النقطة | الحد الأقصى | الفترة الزمنية |
|:---|:---:|:---:|
| تسجيل الدخول | 10 محاولات | 15 دقيقة |
| التسجيل | 5 محاولات | 15 دقيقة |
| API العامة | 100 طلب | دقيقة واحدة |
| تحميل الملفات | 10 طلبات | دقيقة واحدة |

**المتطلبات الإضافية:**
- استخدام `helmet` لجميع رؤوس الأمان.
- تفعيل CORS بشكل صحيح.
- استخدام HTTPS في الإنتاج.

**العقوبة:** رفض الدمج.

---

## 🔵 قواعد البناء والجودة (Build & Quality Rules)

### القاعدة 4: صفر أخطاء TypeScript

> **لا يُسمح بدمج أي كود يحتوي على أخطاء TypeScript أو يستخدم `@ts-nocheck`.**

**التطبيق:**
- يجب أن يمر `tsc --noEmit` بنجاح قبل أي دمج.
- `@ts-nocheck` و `@ts-ignore` محظورة إلا بموافقة مسبقة موثقة.
- إضافة فحص TypeScript إلى CI/CD pipeline.

**الاستثناءات المسموحة (بموافقة):**
- ملفات التكوين الخارجية.
- مكتبات الطرف الثالث بدون أنواع.

**العقوبة:** رفض الدمج.

---

### القاعدة 5: الاختبارات الإلزامية

> **لا يُسمح بدمج أي كود إذا كانت الاختبارات تفشل.**

**المتطلبات:**
- نسبة نجاح الاختبارات: **100%**.
- كل ميزة جديدة يجب أن تأتي مع اختبارات.
- الحد الأدنى لتغطية الكود: **70%**.

**أنواع الاختبارات المطلوبة:**
- اختبارات الوحدة (Unit Tests) للدوال.
- اختبارات التكامل (Integration Tests) للـ API.
- اختبارات E2E للميزات الحرجة.

**العقوبة:** رفض الدمج.

---

### القاعدة 6: حجم الملف

> **لا يُسمح بوجود ملف يتجاوز 500 سطر من الكود.**

**التطبيق:**
- الملفات الكبيرة يجب تقسيمها إلى وحدات أصغر.
- استخدام نمط التصميم المناسب (مثل Repository Pattern).

**الملفات التي تحتاج تقسيم حالياً:**
- `server/db.ts` (4800+ سطر)
- `drizzle/schema.ts` (3200+ سطر)
- `client/src/pages/Dashboard.tsx` (2000+ سطر)

**العقوبة:** تحذير في أول مرة، رفض الدمج في المرات التالية.

---

## 🟢 قواعد الأداء (Performance Rules)

### القاعدة 7: منع N+1 Query

> **يُحظر تماماً تنفيذ استعلامات قاعدة البيانات داخل حلقات `for` أو `forEach` أو `map`.**

**مثال محظور:**
```typescript
// ❌ محظور - N+1 Query
for (const user of users) {
  const orders = await db.select().from(orders).where(eq(orders.userId, user.id));
}
```

**الحل الصحيح:**
```typescript
// ✅ صحيح - استعلام واحد
const userIds = users.map(u => u.id);
const allOrders = await db.select().from(orders).where(inArray(orders.userId, userIds));
```

**العقوبة:** رفض الدمج.

---

### القاعدة 8: تحديد الأعمدة

> **يُحظر استخدام `select()` بدون تحديد الأعمدة المطلوبة.**

**مثال محظور:**
```typescript
// ❌ محظور - يجلب جميع الأعمدة
await db.select().from(users).where(...);
```

**الحل الصحيح:**
```typescript
// ✅ صحيح - يجلب الأعمدة المطلوبة فقط
await db.select({ 
  id: users.id, 
  name: users.nameAr,
  phone: users.phone 
}).from(users).where(...);
```

**الاستثناءات:**
- عند الحاجة الفعلية لجميع الأعمدة (مع توثيق السبب).

**العقوبة:** تحذير في أول مرة، رفض الدمج في المرات التالية.

---

### القاعدة 9: الفهرسة الإلزامية

> **كل عمود يُستخدم في `WHERE` أو `JOIN` أو `ORDER BY` يجب أن يكون له فهرس.**

**التطبيق:**
- تعريف الفهارس في `schema.ts` وليس في ملفات SQL منفصلة.
- مراجعة خطة الاستعلام (Query Plan) للاستعلامات البطيئة.

**مثال:**
```typescript
export const users = mysqlTable("users", {
  id: int("id").primaryKey().autoincrement(),
  phone: varchar("phone", { length: 20 }).notNull(),
  businessId: int("business_id"),
}, (table) => ({
  phoneIdx: index("phone_idx").on(table.phone),
  businessIdx: index("business_idx").on(table.businessId),
}));
```

**العقوبة:** تحذير.

---

### القاعدة 10: تحسين React

> **كل مكون يُعاد تصييره أكثر من 3 مرات في الثانية يجب تحسينه.**

**الأدوات المطلوبة:**
- `React.memo` للمكونات المستقرة.
- `useCallback` للدوال الممررة كـ props.
- `useMemo` للحسابات المكلفة.

**مثال:**
```typescript
// ✅ صحيح
export const DataTable = React.memo(({ data, columns }) => {
  const sortedData = useMemo(() => sortData(data), [data]);
  const handleClick = useCallback((id) => { ... }, []);
  
  return <Table data={sortedData} onClick={handleClick} />;
});
```

**العقوبة:** تحذير.

---

## 🟡 قواعد التوثيق والصيانة (Documentation & Maintenance Rules)
## 🟣 قواعد التطوير المتكامل (Full-Stack Development Rules)

### القاعدة 14: الربط الكامل عند إنشاء الواجهات (Full Integration Rule)

### القاعدة 15: البحث والتحقق قبل البناء (Search Before Build Rule)

### القاعدة 16: التوازي الذكي مع ضمان الدقة والفحص (Smart Parallelism Rule)

> **عند استخدام التوازي الذكي في Manus، يجب ضمان دقة وصحة البيانات المُعطاة للمهام المتوازية، مع إجراء فحص شامل قبل الدمج واختبار الاتصالات الأربعة (CRUD).**

هذه القاعدة تضمن أن التنفيذ المتوازي يتم بشكل **صحيح ودقيق** دون أخطاء ناتجة عن بيانات خاطئة أو دمج غير مفحوص.

**المبدأ الأساسي:**
> "التوازي قوة، لكن بدون دقة وفحص يصبح كارثة. انتبه وركز قبل التوزيع، وافحص قبل الدمج."

---

#### المرحلة 1: قبل التوازي - الانتباه والتركيز

**متطلبات البيانات المُعطاة للتوازي:**

| المتطلب | الوصف |
|:---|:---|
| **الدقة** | البيانات يجب أن تكون صحيحة 100% |
| **الاكتمال** | جميع المعلومات المطلوبة متوفرة |
| **الوضوح** | التعليمات واضحة لا لبس فيها |
| **الاستقلالية** | كل مهمة مستقلة عن الأخرى |

**قائمة التحقق قبل إطلاق التوازي:**

```markdown
## قائمة التحقق قبل التوازي

### دقة البيانات
- [ ] تحققت من صحة أسماء الملفات والمسارات
- [ ] تحققت من صحة أسماء الجداول والأعمدة
- [ ] تحققت من صحة أسماء الدوال والـ Routes
- [ ] تحققت من صحة البيانات المرجعية (IDs, Keys)

### اكتمال المعلومات
- [ ] جميع الحقول المطلوبة محددة
- [ ] جميع العلاقات بين الجداول واضحة
- [ ] جميع قواعد التحقق (Validation) محددة
- [ ] جميع الأنواع (Types) محددة

### استقلالية المهام
- [ ] كل مهمة يمكن تنفيذها بشكل منفصل
- [ ] لا يوجد ترابط بين المهام المتوازية
- [ ] لا يوجد تعارض في الملفات المُعدَّلة
```

---

#### المرحلة 2: أثناء التوازي - المراقبة

**قواعد التنفيذ المتوازي:**

1. **تقسيم المهام بذكاء:**
   - المهام الكبيرة تُقسم إلى دفعات (20 مهمة/دفعة)
   - كل دفعة مستقلة تماماً
   - لا تعارض في الملفات بين الدفعات

2. **توزيع البيانات بدقة:**
   ```typescript
   // ✅ صحيح - بيانات دقيقة ومحددة
   {
     tableName: "products",
     columns: ["id", "name", "price", "categoryId"],
     relations: ["categories"],
     validations: { name: "required", price: "positive" }
   }
   
   // ❌ خطأ - بيانات ناقصة أو غامضة
   {
     table: "prod", // اسم غير دقيق
     fields: "all"  // غير محدد
   }
   ```

---

#### المرحلة 3: بعد التوازي - الفحص قبل الدمج

**الفحص الإلزامي قبل دمج نتائج التوازي:**

##### 1. فحص البناء (Build Check)
```bash
# التأكد من عدم وجود أخطاء بناء
pnpm run check
pnpm run build
```

##### 2. فحص الأنواع (Type Check)
```bash
# التأكد من صحة الأنواع
tsc --noEmit
```

##### 3. فحص التكامل (Integration Check)
- [ ] الملفات المُنشأة متوافقة مع بعضها
- [ ] لا يوجد تعارض في الأسماء
- [ ] الـ Imports صحيحة

##### 4. فحص الاتصالات الأربعة (CRUD Test)

> **إلزامي:** بعد دمج أي ميزة من التوازي، يجب اختبار العمليات الأربع:

| العملية | الاختبار | النتيجة المتوقعة |
|:---:|:---|:---|
| **Create** | إضافة سجل جديد | ✅ السجل يُضاف بنجاح |
| **Read** | عرض السجلات | ✅ البيانات تظهر صحيحة |
| **Update** | تعديل سجل | ✅ التغييرات تُحفظ |
| **Delete** | حذف سجل | ✅ السجل يُحذف |

```markdown
## قائمة فحص CRUD بعد الدمج

### الشاشة: [اسم الشاشة]
- [ ] ✅ Create: أضفت سجل جديد وظهر في القائمة
- [ ] ✅ Read: البيانات تظهر بشكل صحيح
- [ ] ✅ Update: عدّلت سجل وتم حفظ التغييرات
- [ ] ✅ Delete: حذفت سجل واختفى من القائمة

### الاتصال بقاعدة البيانات
- [ ] ✅ الاتصال يعمل بدون أخطاء
- [ ] ✅ البيانات تُحفظ في الجدول الصحيح
- [ ] ✅ العلاقات تعمل بشكل صحيح
```

---

#### ملخص العملية الكاملة

```
┌─────────────────────────────────────────────────────────────┐
│                    قبل التوازي                              │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐         │
│  │ تحقق الدقة  │→│ تحقق الاكتمال│→│ تحقق الاستقلالية│      │
│  └─────────────┘  └─────────────┘  └─────────────┘         │
└─────────────────────────────────────────────────────────────┘
                           ↓
┌─────────────────────────────────────────────────────────────┐
│                    أثناء التوازي                            │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐         │
│  │  الدفعة 1   │  │  الدفعة 2   │  │  الدفعة N   │         │
│  └─────────────┘  └─────────────┘  └─────────────┘         │
└─────────────────────────────────────────────────────────────┘
                           ↓
┌─────────────────────────────────────────────────────────────┐
│                    بعد التوازي                              │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐         │
│  │ فحص البناء  │→│ فحص التكامل │→│ فحص CRUD    │         │
│  └─────────────┘  └─────────────┘  └─────────────┘         │
└─────────────────────────────────────────────────────────────┘
                           ↓
                    ✅ الدمج الآمن
```

**العقوبة:** رفض الدمج - لا يُقبل أي ناتج من التوازي بدون إثبات الفحص واختبار CRUD.

---

> **يُحظر تماماً إنشاء أو بناء أي ميزة أو مكون أو شاشة جديدة قبل البحث والتحقق من أنها غير موجودة مسبقاً في النظام.**

هذه القاعدة تمنع **التكرار والازدواجية** في الكود وتضمن الاستفادة من الميزات الموجودة بدلاً من إعادة اختراع العجلة.

**المبدأ الأساسي:**
> "لا تبني شيئاً جديداً إلا بعد التأكد من عدم وجوده. وإن وُجد، قم بتعديله أو تحسينه بدلاً من تكراره."

**خطوات التحقق الإلزامية قبل البناء:**

#### 1. البحث في الكود المصدري (Code Search)
```bash
# البحث عن الميزة أو المكون في الكود
grep -rn "اسم_الميزة" --include="*.ts" --include="*.tsx"
grep -rn "FeatureName" --include="*.ts" --include="*.tsx"
```

#### 2. البحث في قاعدة البيانات (Database Search)
- [ ] التحقق من `drizzle/schema.ts` للجداول الموجودة
- [ ] التحقق من وجود جدول مشابه أو يخدم نفس الغرض

#### 3. البحث في الواجهات (UI Search)
- [ ] مراجعة `client/src/pages/` للشاشات الموجودة
- [ ] مراجعة `client/src/components/` للمكونات الموجودة

#### 4. البحث في الـ API (Backend Search)
- [ ] مراجعة `server/db.ts` للدوال الموجودة
- [ ] مراجعة `server/routers.ts` للـ Routes الموجودة

#### 5. مراجعة التوثيق (Documentation Review)
- [ ] مراجعة الكتاب المقدس للنظام
- [ ] مراجعة ملفات README و TODO

**قائمة التحقق الإلزامية:**

```markdown
## قائمة التحقق قبل بناء [اسم الميزة]

### البحث والتحقق
- [ ] بحثت في الكود المصدري ولم أجد ميزة مشابهة
- [ ] تحققت من schema.ts ولم أجد جدول مشابه
- [ ] راجعت الشاشات الموجودة ولم أجد شاشة تخدم نفس الغرض
- [ ] راجعت المكونات الموجودة ولم أجد مكون مشابه
- [ ] راجعت دوال الـ API ولم أجد دالة تؤدي نفس الوظيفة

### القرار
- [ ] ✅ الميزة غير موجودة → يمكن البناء من الصفر
- [ ] ⚠️ الميزة موجودة جزئياً → يجب التعديل والإضافة عليها
- [ ] ❌ الميزة موجودة بالكامل → لا حاجة للبناء
```

**السيناريوهات المحتملة:**

| نتيجة البحث | الإجراء المطلوب |
|:---|:---|
| الميزة **غير موجودة** | يمكن البناء من الصفر |
| الميزة **موجودة جزئياً** | التعديل والإضافة على الموجود |
| الميزة **موجودة لكن ناقصة** | إكمال النواقص فقط |
| الميزة **موجودة بالكامل** | عدم البناء - استخدام الموجود |
| يوجد **مكون مشابه** | إعادة استخدامه أو توسيعه |

**أمثلة عملية:**

```typescript
// ❌ خطأ: بناء دالة جديدة دون التحقق
async function getUsers() {
  // قد تكون موجودة بالفعل باسم آخر مثل fetchUsers أو listUsers
}

// ✅ صحيح: البحث أولاً
// 1. grep -rn "getUser\|fetchUser\|listUser" --include="*.ts"
// 2. إذا وجدت: استخدمها أو عدّلها
// 3. إذا لم توجد: أنشئها
```

**الأسئلة الإلزامية قبل البناء:**

1. **هل هذه الميزة موجودة بالفعل؟**
2. **هل يوجد شيء مشابه يمكن تعديله؟**
3. **هل يوجد مكون يمكن إعادة استخدامه؟**
4. **هل الجدول موجود في قاعدة البيانات؟**
5. **هل الـ API موجود ويحتاج فقط لتعديل؟**

**العقوبة:** رفض الدمج - لا يُقبل أي Pull Request لميزة جديدة بدون إثبات إجراء البحث والتحقق.

---

> **عند إنشاء أي واجهة أو شاشة جديدة، يجب إكمال الربط الكامل من الواجهة إلى قاعدة البيانات واختبار جميع العمليات الأساسية (CRUD).**

هذه القاعدة تضمن أن أي شاشة جديدة تكون **وظيفية بالكامل** وليست مجرد واجهة فارغة.

**المتطلبات الإلزامية:**

#### 1. إنشاء/التحقق من جدول قاعدة البيانات (Database Schema)
- [ ] التأكد من وجود الجدول في `drizzle/schema.ts`
- [ ] إضافة الجدول إذا كان غير موجود
- [ ] تشغيل `pnpm db:push` لتطبيق التغييرات

#### 2. إنشاء/التحقق من الـ Backend API
- [ ] إضافة الدوال في `server/db.ts`:
  - `create[Entity]` - للإضافة
  - `get[Entity]` / `get[Entities]` - للعرض
  - `update[Entity]` - للتعديل
  - `delete[Entity]` - للحذف
- [ ] إضافة الـ Routes في `server/routers.ts`:
  - `.mutation` للإضافة والتعديل والحذف
  - `.query` للعرض والقراءة

#### 3. ربط الواجهة الأمامية (Frontend Integration)
- [ ] استخدام `trpc.useQuery` لجلب البيانات
- [ ] استخدام `trpc.useMutation` للعمليات
- [ ] إضافة معالجة الأخطاء والتحميل

#### 4. اختبار العمليات الأربع (CRUD Testing)
- [ ] **إضافة (Create):** اختبار إضافة سجل جديد والتأكد من ظهوره
- [ ] **عرض (Read):** اختبار عرض البيانات بشكل صحيح
- [ ] **تعديل (Update):** اختبار تعديل سجل والتأكد من حفظ التغييرات
- [ ] **حذف (Delete):** اختبار حذف سجل والتأكد من إزالته

**قائمة التحقق الإلزامية (Checklist):**

```markdown
## قائمة التحقق لشاشة [اسم الشاشة]

### قاعدة البيانات
- [ ] الجدول موجود في schema.ts
- [ ] تم تشغيل db:push

### الباك إند
- [ ] دالة الإضافة (create)
- [ ] دالة العرض (get/list)
- [ ] دالة التعديل (update)
- [ ] دالة الحذف (delete)
- [ ] Routes مضافة في routers.ts

### الفرونت إند
- [ ] الشاشة مربوطة بـ useQuery
- [ ] الشاشة مربوطة بـ useMutation
- [ ] معالجة حالة التحميل
- [ ] معالجة حالة الخطأ

### الاختبار
- [ ] ✅ اختبار الإضافة - يعمل
- [ ] ✅ اختبار العرض - يعمل
- [ ] ✅ اختبار التعديل - يعمل
- [ ] ✅ اختبار الحذف - يعمل
```

**مثال على التطبيق الصحيح:**

```typescript
// 1. Schema (drizzle/schema.ts)
export const products = mysqlTable("products", {
  id: int("id").primaryKey().autoincrement(),
  name: varchar("name", { length: 255 }).notNull(),
  price: decimal("price", { precision: 10, scale: 2 }),
  createdAt: timestamp("created_at").defaultNow(),
});

// 2. Backend (server/db.ts)
export async function createProduct(data: InsertProduct) {
  const db = await getDb();
  return await db.insert(products).values(data);
}

export async function getProducts() {
  const db = await getDb();
  return await db.select().from(products);
}

export async function updateProduct(id: number, data: Partial<InsertProduct>) {
  const db = await getDb();
  return await db.update(products).set(data).where(eq(products.id, id));
}

export async function deleteProduct(id: number) {
  const db = await getDb();
  return await db.delete(products).where(eq(products.id, id));
}

// 3. Routes (server/routers.ts)
products: {
  list: publicProcedure.query(() => getProducts()),
  create: publicProcedure.input(insertProductSchema).mutation(({ input }) => createProduct(input)),
  update: publicProcedure.input(updateProductSchema).mutation(({ input }) => updateProduct(input.id, input)),
  delete: publicProcedure.input(z.number()).mutation(({ input }) => deleteProduct(input)),
}

// 4. Frontend (client/src/pages/Products.tsx)
const { data, isLoading } = trpc.products.list.useQuery();
const createMutation = trpc.products.create.useMutation();
const updateMutation = trpc.products.update.useMutation();
const deleteMutation = trpc.products.delete.useMutation();
```

**العقوبة:** رفض الدمج - لا يُقبل أي Pull Request لشاشة جديدة بدون إكمال جميع عناصر القائمة.

---

### القاعدة 11: التعليقات المحظورة

> **يُحظر ترك تعليقات `TODO` أو `FIXME` في الكود لأكثر من أسبوعين.**

**التطبيق:**
- كل `TODO` يجب أن يتحول إلى Issue في GitHub.
- مراجعة أسبوعية للتعليقات المعلقة.
- تحديد مسؤول وتاريخ لكل TODO.

**الصيغة المطلوبة:**
```typescript
// TODO(@username, 2025-01-01): وصف المهمة - Issue #123
```

**العقوبة:** تحذير.

---

### القاعدة 12: منع console.log

> **يُحظر وجود `console.log` في كود الإنتاج.**

**التطبيق:**
- استخدام نظام تسجيل (Logger) مثل `winston` أو `pino`.
- إضافة قاعدة ESLint لمنع `console.log`.

**البديل الصحيح:**
```typescript
// ❌ محظور
console.log("User logged in:", userId);

// ✅ صحيح
logger.info("User logged in", { userId });
```

**العقوبة:** تحذير في أول مرة، رفض الدمج في المرات التالية.

---

### القاعدة 13: التبعيات الآمنة

> **لا يُسمح بوجود ثغرات أمنية عالية أو حرجة في التبعيات.**

**جدول المعالجة:**

| مستوى الخطورة | المدة المسموحة للإصلاح |
|:---|:---:|
| حرجة (Critical) | فوراً |
| عالية (High) | 7 أيام |
| متوسطة (Moderate) | 30 يوماً |
| منخفضة (Low) | 90 يوماً |

**التطبيق:**
- تشغيل `pnpm audit` أسبوعياً.
- تحديث التبعيات التي تحتوي على ثغرات.

**العقوبة:** إلزامي حسب الجدول.

---

## 📋 ملخص القواعد والعقوبات

| # | القاعدة | الفئة | العقوبة |
|:---:|:---|:---|:---|
| 1 | لا أسرار في الكود | الأمان | رفض فوري |
| 2 | لا ملفات حساسة | الأمان | رفض فوري |
| 3 | Rate Limiting إلزامي | الأمان | رفض |
| 4 | صفر أخطاء TypeScript | الجودة | رفض |
| 5 | 100% نجاح الاختبارات | الجودة | رفض |
| 6 | حد 500 سطر للملف | الجودة | تحذير → رفض |
| 7 | منع N+1 Query | الأداء | رفض |
| 8 | تحديد أعمدة SELECT | الأداء | تحذير → رفض |
| 9 | فهرسة الأعمدة | الأداء | تحذير |
| 10 | تحسين React | الأداء | تحذير |
| 11 | حذف TODO القديمة | الصيانة | تحذير |
| 12 | منع console.log | الصيانة | تحذير → رفض |
| 13 | تحديث التبعيات | الصيانة | إلزامي |
| 14 | الربط الكامل للواجهات | التطوير المتكامل | رفض |
| 15 | البحث والتحقق قبل البناء | التطوير المتكامل | رفض |
| 16 | التوازي الذكي مع الفحص | التطوير المتكامل | رفض |

---

## 🔄 منهجية الترحيل والتحقق

عند إتمام أي مهمة، يجب اتباع الخطوات التالية بشكل صارم:

1. **الكومنت (Commit):** القيام بعملية الكومنت للتغييرات.
2. **تحديث الإصدار (Update Version):** تحديث رقم الإصدار (الرقم الثالث - Patch).
3. **الرفع (Push):** رفع الكومنت إلى مستودع Git.
4. **النشر (Publishing):** نشر التغييرات على بيئة الاستضافة.
5. **التحقق من النشر (Browser Test):** فتح المتصفح والتحقق من الإضافات.
6. **اختبار الشاشات (Screen Test):** إجراء اختبار شامل للشاشات والإضافات الجديدة.

---

## 📝 التوقيع والالتزام

بالمساهمة في هذا المستودع، أنت توافق على الالتزام بجميع القواعد المذكورة أعلاه.

---

**تم إعداد هذه الوثيقة بواسطة:** Manus AI  
**بناءً على:** فحص شامل للمستودع وتحليل الفجوات

---

## 🔵 فئة: التحكم في الإصدارات والنشر (Version Control & Deployment)

### القاعدة 17: الإصدارات الدلالية (Semantic Versioning Rule)

> **يجب اتباع نظام الإصدارات الدلالية (Semantic Versioning) في جميع التحديثات: MAJOR.MINOR.PATCH**

**المبدأ الأساسي:**
> "كل تغيير له رقم، وكل رقم له معنى."

**نظام الترقيم:**

| النوع | متى يُستخدم | مثال |
|:---:|:---|:---|
| **MAJOR** | تغييرات جذرية غير متوافقة | 1.0.0 → 2.0.0 |
| **MINOR** | إضافة ميزات جديدة متوافقة | 1.0.0 → 1.1.0 |
| **PATCH** | إصلاح أخطاء فقط | 1.0.0 → 1.0.1 |

**قواعد التحديث:**
```typescript
// في package.json
{
  "version": "1.2.3"  // MAJOR.MINOR.PATCH
}

// متى تُحدّث كل رقم:
// MAJOR: حذف دالة، تغيير API، تغيير schema
// MINOR: إضافة شاشة، إضافة ميزة، إضافة endpoint
// PATCH: إصلاح bug، تحسين أداء، تصحيح نص
```

**العقوبة:** رفض الدمج - لا يُقبل أي تغيير بدون تحديث رقم الإصدار.

---

### القاعدة 18: سجل التغييرات (Changelog Rule)

> **كل تغيير يجب توثيقه في ملف CHANGELOG.md قبل الدمج.**

**المبدأ الأساسي:**
> "ما لم يُوثّق، لم يحدث."

**هيكل ملف CHANGELOG.md:**
```markdown
# سجل التغييرات (Changelog)

## [1.3.0] - 2025-12-24

### ✨ إضافات (Added)
- إضافة شاشة إدارة المستخدمين
- إضافة تصدير PDF للتقارير

### 🔧 تعديلات (Changed)
- تحسين أداء استعلامات قاعدة البيانات
- تحديث واجهة لوحة التحكم

### 🐛 إصلاحات (Fixed)
- إصلاح مشكلة تسجيل الدخول
- إصلاح خطأ في حساب الإجماليات

### 🗑️ حذف (Removed)
- حذف الميزة القديمة X

### ⚠️ تنبيهات (Deprecated)
- الدالة Y ستُحذف في الإصدار 2.0.0
```

**العقوبة:** رفض الدمج - لا يُقبل أي PR بدون تحديث CHANGELOG.

---

### القاعدة 19: منهجية الترحيل والنشر (Deployment Pipeline Rule)

> **يجب اتباع خطوات الترحيل بالترتيب الصارم: Commit → Version → Push → Deploy → Verify**

**خطوات الترحيل الإلزامية:**

```
┌─────────────────────────────────────────────────────────────┐
│  1. Commit    →  2. Version  →  3. Push  →  4. Deploy      │
│     ↓              ↓              ↓            ↓            │
│  حفظ التغييرات   تحديث الرقم    رفع للـGit   نشر للإنتاج    │
└─────────────────────────────────────────────────────────────┘
                           ↓
┌─────────────────────────────────────────────────────────────┐
│  5. Browser Test  →  6. CRUD Test  →  7. Smoke Test        │
│        ↓                  ↓                ↓                │
│   فتح المتصفح        اختبار CRUD      اختبار سريع          │
└─────────────────────────────────────────────────────────────┘
```

**قائمة التحقق:**
- [ ] تم عمل Commit برسالة واضحة
- [ ] تم تحديث رقم الإصدار
- [ ] تم تحديث CHANGELOG
- [ ] تم الرفع إلى GitHub
- [ ] تم النشر على الاستضافة
- [ ] تم التحقق من المتصفح
- [ ] تم اختبار CRUD
- [ ] تم اختبار الميزات الرئيسية

**العقوبة:** إعادة الترحيل - أي خطوة مفقودة تتطلب إعادة العملية.

---

## 🟡 فئة: جودة الكود المتقدمة (Advanced Code Quality)

### القاعدة 20: مراجعة الكود (Code Review Rule)

> **لا يُدمج أي كود في الفرع الرئيسي بدون مراجعة من شخص آخر أو فحص آلي.**

**معايير المراجعة:**

| الجانب | ما يُفحص |
|:---|:---|
| **الوظيفة** | هل الكود يعمل كما هو متوقع؟ |
| **القراءة** | هل الكود سهل الفهم؟ |
| **الأمان** | هل هناك ثغرات أمنية؟ |
| **الأداء** | هل هناك مشاكل أداء؟ |
| **الاختبار** | هل الكود مُختبر؟ |

**قائمة فحص المراجعة:**
```markdown
## مراجعة الكود - PR #XXX

### الوظيفة
- [ ] الكود يحقق المطلوب
- [ ] لا يوجد أخطاء منطقية
- [ ] الحالات الحدية مُعالجة

### الجودة
- [ ] التسمية واضحة ومتسقة
- [ ] لا يوجد كود مكرر
- [ ] الدوال قصيرة ومركزة

### الأمان
- [ ] المدخلات مُتحقق منها
- [ ] لا يوجد أسرار مكشوفة
- [ ] الصلاحيات مُطبقة

### الأداء
- [ ] لا يوجد N+1 Query
- [ ] الاستعلامات محسّنة
- [ ] لا يوجد تسريب ذاكرة
```

**العقوبة:** رفض الدمج - لا يُقبل أي PR بدون مراجعة.

---

### القاعدة 21: تغطية الاختبارات (Test Coverage Rule)

> **الحد الأدنى لتغطية الاختبارات هو 80% للكود الجديد.**

**أنواع الاختبارات المطلوبة:**

| النوع | الغرض | الحد الأدنى |
|:---|:---|:---:|
| **Unit Tests** | اختبار الدوال المنفردة | 80% |
| **Integration Tests** | اختبار التكامل بين المكونات | 70% |
| **E2E Tests** | اختبار السيناريوهات الكاملة | 50% |

**مثال على اختبار جيد:**
```typescript
describe('UserService', () => {
  describe('createUser', () => {
    it('should create user with valid data', async () => {
      const user = await userService.createUser({
        name: 'Test User',
        email: 'test@example.com'
      });
      expect(user.id).toBeDefined();
      expect(user.name).toBe('Test User');
    });

    it('should reject invalid email', async () => {
      await expect(userService.createUser({
        name: 'Test',
        email: 'invalid'
      })).rejects.toThrow('Invalid email');
    });

    it('should reject duplicate email', async () => {
      // ... اختبار البريد المكرر
    });
  });
});
```

**العقوبة:** رفض الدمج - لا يُقبل كود جديد بتغطية أقل من 80%.

---

### القاعدة 22: التسمية الموحدة (Naming Convention Rule)

> **يجب اتباع معايير تسمية صارمة وموحدة في جميع أنحاء المشروع.**

**معايير التسمية:**

| العنصر | النمط | مثال |
|:---|:---|:---|
| **الملفات (Components)** | PascalCase | `UserProfile.tsx` |
| **الملفات (Utilities)** | camelCase | `formatDate.ts` |
| **الدوال** | camelCase | `getUserById()` |
| **المتغيرات** | camelCase | `userName` |
| **الثوابت** | UPPER_SNAKE_CASE | `MAX_RETRY_COUNT` |
| **الجداول** | snake_case | `user_profiles` |
| **الأعمدة** | snake_case | `created_at` |
| **الـ CSS Classes** | kebab-case | `user-profile-card` |
| **الـ Interfaces** | PascalCase + I prefix | `IUserProfile` |
| **الـ Types** | PascalCase | `UserProfile` |
| **الـ Enums** | PascalCase | `UserStatus` |

**أمثلة:**
```typescript
// ✅ صحيح
const MAX_RETRY_COUNT = 3;
const userName = 'Ahmed';
function getUserById(id: number): User { }
interface IUserProfile { }
type UserStatus = 'active' | 'inactive';

// ❌ خطأ
const max_retry_count = 3;  // يجب UPPER_SNAKE_CASE
const UserName = 'Ahmed';   // يجب camelCase
function get_user_by_id() { }  // يجب camelCase
```

**العقوبة:** رفض الدمج - أي انتهاك للتسمية يُرفض.

---

### القاعدة 23: حجم الدوال والملفات (Size Limits Rule)

> **يجب الالتزام بحدود صارمة لحجم الدوال والملفات.**

**الحدود المسموحة:**

| العنصر | الحد الأقصى | السبب |
|:---|:---:|:---|
| **سطور الدالة** | 50 سطر | سهولة الفهم والاختبار |
| **سطور الملف** | 300 سطر | سهولة الصيانة |
| **معاملات الدالة** | 4 معاملات | تقليل التعقيد |
| **مستوى التداخل** | 3 مستويات | سهولة القراءة |

**كيفية التقسيم:**
```typescript
// ❌ خطأ - دالة طويلة جداً
async function processOrder(order) {
  // 100+ سطر من الكود
}

// ✅ صحيح - تقسيم إلى دوال صغيرة
async function processOrder(order) {
  validateOrder(order);
  const total = calculateTotal(order);
  await saveOrder(order, total);
  await sendNotification(order);
}

function validateOrder(order) { /* 10 سطور */ }
function calculateTotal(order) { /* 15 سطر */ }
async function saveOrder(order, total) { /* 20 سطر */ }
async function sendNotification(order) { /* 10 سطور */ }
```

**العقوبة:** تحذير ثم رفض - الملفات الكبيرة يجب تقسيمها.

---

## 🔴 فئة: الأمان المتقدم (Advanced Security)

### القاعدة 24: التحقق من المدخلات (Input Validation Rule)

> **كل مدخل من المستخدم يجب التحقق منه باستخدام Zod أو مكتبة مشابهة.**

**المبدأ الأساسي:**
> "لا تثق بأي مدخل من المستخدم. تحقق من كل شيء."

**مثال على التحقق الصحيح:**
```typescript
import { z } from 'zod';

// تعريف Schema للتحقق
const createUserSchema = z.object({
  name: z.string()
    .min(2, 'الاسم قصير جداً')
    .max(100, 'الاسم طويل جداً'),
  email: z.string()
    .email('البريد الإلكتروني غير صالح'),
  age: z.number()
    .min(18, 'يجب أن يكون العمر 18 أو أكثر')
    .max(120, 'العمر غير صالح'),
  password: z.string()
    .min(8, 'كلمة المرور قصيرة')
    .regex(/[A-Z]/, 'يجب أن تحتوي على حرف كبير')
    .regex(/[0-9]/, 'يجب أن تحتوي على رقم')
});

// استخدام في الـ Route
app.post('/users', async (req, res) => {
  const result = createUserSchema.safeParse(req.body);
  if (!result.success) {
    return res.status(400).json({ errors: result.error.errors });
  }
  // المدخلات آمنة الآن
  const user = await createUser(result.data);
});
```

**قائمة التحقق:**
- [ ] جميع المدخلات لها Schema
- [ ] الأنواع محددة (string, number, etc.)
- [ ] الحدود محددة (min, max)
- [ ] الأنماط محددة (email, url, regex)
- [ ] الرسائل بالعربية واضحة

**العقوبة:** رفض الدمج - لا يُقبل أي endpoint بدون validation.

---

### القاعدة 25: سجلات التدقيق (Audit Logs Rule)

> **يجب تسجيل جميع العمليات الحساسة: من فعل، متى، ماذا فعل، على ماذا.**

**العمليات التي يجب تسجيلها:**

| العملية | مستوى الأهمية |
|:---|:---:|
| تسجيل الدخول/الخروج | 🔴 حرج |
| تغيير كلمة المرور | 🔴 حرج |
| إضافة/حذف مستخدم | 🔴 حرج |
| تعديل الصلاحيات | 🔴 حرج |
| حذف بيانات | 🟡 مهم |
| تعديل إعدادات | 🟡 مهم |
| تصدير بيانات | 🟡 مهم |
| عمليات مالية | 🔴 حرج |

**هيكل سجل التدقيق:**
```typescript
interface AuditLog {
  id: string;
  timestamp: Date;
  userId: string;
  userName: string;
  action: string;        // 'CREATE' | 'UPDATE' | 'DELETE' | 'LOGIN' | 'EXPORT'
  resource: string;      // 'user' | 'order' | 'settings'
  resourceId: string;
  oldValue?: object;     // القيمة القديمة (للتعديل)
  newValue?: object;     // القيمة الجديدة
  ipAddress: string;
  userAgent: string;
  status: 'success' | 'failure';
  errorMessage?: string;
}

// مثال على التسجيل
await auditLog.create({
  userId: currentUser.id,
  userName: currentUser.name,
  action: 'DELETE',
  resource: 'user',
  resourceId: deletedUserId,
  oldValue: deletedUserData,
  ipAddress: req.ip,
  userAgent: req.headers['user-agent'],
  status: 'success'
});
```

**العقوبة:** رفض الدمج - العمليات الحساسة بدون تسجيل تُرفض.

---

### القاعدة 26: عدم كشف الأخطاء الداخلية (Error Masking Rule)

> **الأخطاء التقنية الداخلية يجب ألا تظهر للمستخدم النهائي.**

**المبدأ الأساسي:**
> "المستخدم يرى رسالة ودية، المطور يرى التفاصيل في السجلات."

**مثال على معالجة الأخطاء:**
```typescript
// ❌ خطأ - كشف تفاصيل داخلية
app.get('/users/:id', async (req, res) => {
  try {
    const user = await getUser(req.params.id);
    res.json(user);
  } catch (error) {
    res.status(500).json({ 
      error: error.message,  // قد يكشف معلومات حساسة!
      stack: error.stack     // خطير جداً!
    });
  }
});

// ✅ صحيح - إخفاء التفاصيل
app.get('/users/:id', async (req, res) => {
  try {
    const user = await getUser(req.params.id);
    res.json(user);
  } catch (error) {
    // تسجيل الخطأ الكامل للمطور
    logger.error('Failed to get user', {
      userId: req.params.id,
      error: error.message,
      stack: error.stack
    });
    
    // رسالة ودية للمستخدم
    res.status(500).json({ 
      error: 'حدث خطأ أثناء جلب البيانات. يرجى المحاولة لاحقاً.',
      errorCode: 'USER_FETCH_ERROR',
      requestId: req.id  // للدعم الفني
    });
  }
});
```

**العقوبة:** رفض الدمج - أي كشف لـ stack trace أو تفاصيل داخلية يُرفض.

---

### القاعدة 27: حماية الـ API (API Security Rule)

> **جميع نقاط الـ API يجب أن تكون محمية بالمصادقة والتفويض المناسب.**

**طبقات الحماية:**

```
┌─────────────────────────────────────────────────────────────┐
│                    طلب من المستخدم                          │
└─────────────────────────────────────────────────────────────┘
                           ↓
┌─────────────────────────────────────────────────────────────┐
│  1. Rate Limiting     - تحديد عدد الطلبات                  │
└─────────────────────────────────────────────────────────────┘
                           ↓
┌─────────────────────────────────────────────────────────────┐
│  2. Authentication    - التحقق من الهوية (JWT)             │
└─────────────────────────────────────────────────────────────┘
                           ↓
┌─────────────────────────────────────────────────────────────┐
│  3. Authorization     - التحقق من الصلاحيات                │
└─────────────────────────────────────────────────────────────┘
                           ↓
┌─────────────────────────────────────────────────────────────┐
│  4. Input Validation  - التحقق من المدخلات                 │
└─────────────────────────────────────────────────────────────┘
                           ↓
┌─────────────────────────────────────────────────────────────┐
│  5. Business Logic    - تنفيذ العملية                      │
└─────────────────────────────────────────────────────────────┘
```

**مثال على الحماية الكاملة:**
```typescript
router.delete('/users/:id',
  rateLimit({ max: 10, windowMs: 60000 }),  // 1. Rate Limiting
  authenticate,                               // 2. Authentication
  authorize(['admin']),                       // 3. Authorization
  validateParams(deleteUserSchema),           // 4. Validation
  async (req, res) => {                       // 5. Business Logic
    await deleteUser(req.params.id);
    res.json({ success: true });
  }
);
```

**العقوبة:** رفض الدمج - أي endpoint بدون حماية كاملة يُرفض.

---

## 🟢 فئة: إدارة الأخطاء (Error Management)

### القاعدة 28: معالجة الأخطاء الموحدة (Unified Error Handling Rule)

> **كل خطأ يجب أن يُعالج برسالة واضحة وكود محدد وهيكل موحد.**

**هيكل الخطأ الموحد:**
```typescript
interface ApiError {
  success: false;
  error: {
    code: string;           // كود فريد للخطأ
    message: string;        // رسالة للمستخدم (بالعربية)
    details?: object;       // تفاصيل إضافية (اختياري)
    field?: string;         // الحقل المتسبب (للتحقق)
  };
  requestId: string;        // للدعم الفني
  timestamp: string;
}

// أمثلة على أكواد الأخطاء
const ErrorCodes = {
  // أخطاء المصادقة (AUTH_XXX)
  AUTH_INVALID_CREDENTIALS: 'بيانات الدخول غير صحيحة',
  AUTH_TOKEN_EXPIRED: 'انتهت صلاحية الجلسة',
  AUTH_UNAUTHORIZED: 'غير مصرح لك بهذه العملية',
  
  // أخطاء التحقق (VAL_XXX)
  VAL_REQUIRED_FIELD: 'هذا الحقل مطلوب',
  VAL_INVALID_EMAIL: 'البريد الإلكتروني غير صالح',
  VAL_MIN_LENGTH: 'النص قصير جداً',
  
  // أخطاء الموارد (RES_XXX)
  RES_NOT_FOUND: 'العنصر غير موجود',
  RES_ALREADY_EXISTS: 'العنصر موجود مسبقاً',
  RES_CONFLICT: 'تعارض في البيانات',
  
  // أخطاء النظام (SYS_XXX)
  SYS_DATABASE_ERROR: 'خطأ في قاعدة البيانات',
  SYS_INTERNAL_ERROR: 'خطأ داخلي في النظام',
  SYS_SERVICE_UNAVAILABLE: 'الخدمة غير متاحة حالياً'
};
```

**العقوبة:** رفض الدمج - أي خطأ بدون كود وهيكل موحد يُرفض.

---

### القاعدة 29: التعافي من الأخطاء (Error Recovery Rule)

> **النظام يجب أن يتعافى بذكاء من الأخطاء دون توقف كامل.**

**استراتيجيات التعافي:**

| الخطأ | الاستراتيجية |
|:---|:---|
| فشل الاتصال بقاعدة البيانات | إعادة المحاولة 3 مرات مع تأخير |
| فشل API خارجي | استخدام Cache أو قيمة افتراضية |
| خطأ في معالجة طلب | عزل الخطأ وإكمال الباقي |
| نفاد الذاكرة | تنظيف Cache وتحرير الموارد |

**مثال على إعادة المحاولة:**
```typescript
async function withRetry<T>(
  fn: () => Promise<T>,
  maxRetries = 3,
  delay = 1000
): Promise<T> {
  for (let attempt = 1; attempt <= maxRetries; attempt++) {
    try {
      return await fn();
    } catch (error) {
      if (attempt === maxRetries) throw error;
      
      logger.warn(`Attempt ${attempt} failed, retrying...`, { error });
      await sleep(delay * attempt);  // تأخير متزايد
    }
  }
  throw new Error('Max retries exceeded');
}

// الاستخدام
const data = await withRetry(() => fetchFromDatabase(id));
```

**العقوبة:** تحذير - العمليات الحرجة بدون retry تُراجع.

---

## 🟣 فئة: الأداء المتقدم (Advanced Performance)

### القاعدة 30: التخزين المؤقت (Caching Rule)

> **البيانات المتكررة يجب تخزينها مؤقتاً لتحسين الأداء.**

**استراتيجية التخزين المؤقت:**

| نوع البيانات | مدة التخزين | المكان |
|:---|:---:|:---|
| بيانات ثابتة (دول، عملات) | 24 ساعة | Memory + Redis |
| قوائم (منتجات، فئات) | 1 ساعة | Redis |
| بيانات مستخدم | 15 دقيقة | Memory |
| نتائج بحث | 5 دقائق | Redis |
| بيانات حساسة | لا تُخزّن | - |

**مثال على التخزين المؤقت:**
```typescript
// إعداد Cache
const cache = new Map<string, { data: any; expiry: number }>();

async function getCached<T>(
  key: string,
  fetcher: () => Promise<T>,
  ttlSeconds = 300
): Promise<T> {
  const cached = cache.get(key);
  
  if (cached && cached.expiry > Date.now()) {
    return cached.data as T;
  }
  
  const data = await fetcher();
  cache.set(key, {
    data,
    expiry: Date.now() + (ttlSeconds * 1000)
  });
  
  return data;
}

// الاستخدام
const categories = await getCached(
  'categories:all',
  () => db.select().from(categories),
  3600  // ساعة واحدة
);
```

**العقوبة:** تحذير - الاستعلامات المتكررة بدون cache تُراجع.

---

### القاعدة 31: التحميل الكسول (Lazy Loading Rule)

> **المكونات والبيانات يجب تحميلها عند الحاجة فقط.**

**تطبيق Lazy Loading:**

```typescript
// ✅ صحيح - تحميل كسول للصفحات
const Dashboard = lazy(() => import('./pages/Dashboard'));
const Reports = lazy(() => import('./pages/Reports'));
const Settings = lazy(() => import('./pages/Settings'));

function App() {
  return (
    <Suspense fallback={<LoadingSpinner />}>
      <Routes>
        <Route path="/dashboard" element={<Dashboard />} />
        <Route path="/reports" element={<Reports />} />
        <Route path="/settings" element={<Settings />} />
      </Routes>
    </Suspense>
  );
}

// ✅ صحيح - تحميل كسول للبيانات
function UserProfile({ userId }) {
  const { data, isLoading } = useQuery({
    queryKey: ['user', userId],
    queryFn: () => fetchUser(userId),
    enabled: !!userId,  // لا تحمّل إلا عند وجود ID
    staleTime: 5 * 60 * 1000  // 5 دقائق
  });
}
```

**العقوبة:** تحذير - الصفحات الكبيرة بدون lazy loading تُراجع.

---

### القاعدة 32: تحسين الاستعلامات (Query Optimization Rule)

> **كل استعلام يجب أن يكون محسّناً: محدد الأعمدة، مفهرس، مُرقّم.**

**قواعد الاستعلامات:**

```typescript
// ❌ خطأ - استعلام غير محسّن
const users = await db.select().from(users);  // كل الأعمدة!
const orders = await db.select().from(orders).where(eq(orders.userId, id));  // بدون فهرس!

// ✅ صحيح - استعلام محسّن
const users = await db
  .select({
    id: users.id,
    name: users.name,
    email: users.email
  })
  .from(users)
  .limit(20)
  .offset(page * 20);

// مع الفهرسة في schema
export const orders = mysqlTable('orders', {
  id: int('id').primaryKey(),
  userId: int('user_id').notNull(),
  // ...
}, (table) => ({
  userIdIdx: index('user_id_idx').on(table.userId)  // فهرس!
}));
```

**العقوبة:** رفض الدمج - استعلامات بدون تحديد أعمدة أو pagination تُرفض.

---

## 🟠 فئة: التوثيق والصيانة (Documentation & Maintenance)

### القاعدة 33: توثيق الـ API (API Documentation Rule)

> **كل endpoint يجب أن يكون موثقاً بالكامل: المسار، الطريقة، المدخلات، المخرجات، الأخطاء.**

**هيكل التوثيق:**
```typescript
/**
 * @api {post} /api/users إنشاء مستخدم جديد
 * @apiName CreateUser
 * @apiGroup Users
 * @apiVersion 1.0.0
 * 
 * @apiHeader {String} Authorization Bearer token
 * 
 * @apiBody {String} name اسم المستخدم (2-100 حرف)
 * @apiBody {String} email البريد الإلكتروني
 * @apiBody {String} password كلمة المرور (8+ حروف)
 * 
 * @apiSuccess {Object} user بيانات المستخدم
 * @apiSuccess {Number} user.id معرف المستخدم
 * @apiSuccess {String} user.name اسم المستخدم
 * @apiSuccess {String} user.email البريد الإلكتروني
 * 
 * @apiError (400) VAL_INVALID_EMAIL البريد الإلكتروني غير صالح
 * @apiError (409) RES_ALREADY_EXISTS البريد مستخدم مسبقاً
 * @apiError (401) AUTH_UNAUTHORIZED غير مصرح
 * 
 * @apiExample {curl} مثال:
 *     curl -X POST https://api.example.com/users \
 *       -H "Authorization: Bearer xxx" \
 *       -d '{"name":"أحمد","email":"ahmed@example.com","password":"Pass123!"}'
 */
```

**العقوبة:** رفض الدمج - أي endpoint جديد بدون توثيق يُرفض.

---

### القاعدة 34: التعليقات الذكية (Smart Comments Rule)

> **التعليقات يجب أن تشرح "لماذا" وليس "ماذا".**

**أمثلة:**
```typescript
// ❌ خطأ - تعليق يشرح "ماذا" (واضح من الكود)
// زيادة العداد بواحد
counter++;

// ✅ صحيح - تعليق يشرح "لماذا"
// نبدأ من 1 وليس 0 لأن الـ API الخارجي يتوقع ترقيم يبدأ من 1
counter++;

// ❌ خطأ - تعليق غير مفيد
// حلقة for
for (let i = 0; i < items.length; i++) { }

// ✅ صحيح - تعليق يشرح السبب
// نستخدم for بدلاً من forEach للتمكن من الخروج مبكراً عند إيجاد العنصر
for (let i = 0; i < items.length; i++) {
  if (items[i].id === targetId) break;
}

// ✅ ممتاز - تعليق يشرح قرار تصميمي
/**
 * نستخدم debounce بـ 300ms لأن:
 * 1. المستخدم يكتب بمعدل 200ms بين الأحرف
 * 2. نريد تقليل الطلبات للـ API
 * 3. 300ms توازن جيد بين الاستجابة وتقليل الحمل
 */
const debouncedSearch = useMemo(
  () => debounce(handleSearch, 300),
  [handleSearch]
);
```

**العقوبة:** تحذير - التعليقات غير المفيدة تُطلب إزالتها أو تحسينها.

---

### القاعدة 35: النسخ الاحتياطي (Backup Strategy Rule)

> **يجب وجود خطة واضحة للنسخ الاحتياطي واستعادة البيانات.**

**خطة النسخ الاحتياطي:**

| النوع | التكرار | الاحتفاظ | المكان |
|:---|:---:|:---:|:---|
| **Full Backup** | يومياً | 30 يوم | Cloud Storage |
| **Incremental** | كل 6 ساعات | 7 أيام | Cloud Storage |
| **Transaction Log** | كل ساعة | 24 ساعة | Local + Cloud |

**قائمة التحقق:**
- [ ] النسخ الاحتياطي يعمل تلقائياً
- [ ] تم اختبار الاستعادة (Restore Test)
- [ ] النسخ مشفرة
- [ ] النسخ في موقع جغرافي مختلف
- [ ] تنبيهات عند فشل النسخ

**العقوبة:** تحذير حرج - النظام بدون خطة backup يُوقف حتى الإصلاح.

---

## 🔷 فئة: تجربة المستخدم (User Experience)

### القاعدة 36: التغذية الراجعة الفورية (Instant Feedback Rule)

> **كل إجراء من المستخدم يجب أن يحصل على تغذية راجعة فورية.**

**أنواع التغذية الراجعة:**

| الإجراء | التغذية الراجعة |
|:---|:---|
| نقر زر | تغيير لون + loading |
| إرسال نموذج | رسالة نجاح/خطأ |
| تحميل بيانات | skeleton أو spinner |
| حذف عنصر | تأكيد + رسالة نجاح |
| خطأ في الإدخال | رسالة تحت الحقل فوراً |

**مثال:**
```typescript
function SubmitButton({ isLoading, onClick }) {
  return (
    <button 
      onClick={onClick}
      disabled={isLoading}
      className={isLoading ? 'opacity-50' : ''}
    >
      {isLoading ? (
        <>
          <Spinner className="animate-spin" />
          جارٍ الحفظ...
        </>
      ) : (
        'حفظ'
      )}
    </button>
  );
}
```

**العقوبة:** تحذير - أي إجراء بدون feedback يُطلب تحسينه.

---

### القاعدة 37: رسائل الخطأ الودية (Friendly Error Messages Rule)

> **رسائل الخطأ يجب أن تكون واضحة، ودية، وتقدم حلاً.**

**هيكل الرسالة الجيدة:**
```
[ماذا حدث] + [لماذا حدث (اختياري)] + [ماذا تفعل]
```

**أمثلة:**
```typescript
// ❌ خطأ - رسائل غير مفيدة
"Error 500"
"Invalid input"
"Something went wrong"

// ✅ صحيح - رسائل ودية ومفيدة
"لم نتمكن من حفظ البيانات. يرجى التحقق من اتصالك بالإنترنت والمحاولة مرة أخرى."
"البريد الإلكتروني غير صالح. يرجى إدخال بريد بصيغة example@domain.com"
"كلمة المرور ضعيفة. يجب أن تحتوي على 8 أحرف على الأقل، حرف كبير، ورقم."
"هذا البريد مسجل مسبقاً. هل تريد تسجيل الدخول بدلاً من ذلك؟"
```

**العقوبة:** تحذير - الرسائل غير الودية تُطلب تحسينها.

---

### القاعدة 38: إمكانية الوصول (Accessibility Rule)

> **النظام يجب أن يكون قابلاً للاستخدام من الجميع، بما في ذلك ذوي الاحتياجات الخاصة.**

**متطلبات إمكانية الوصول:**

| المتطلب | التطبيق |
|:---|:---|
| **تباين الألوان** | نسبة 4.5:1 على الأقل |
| **حجم الخط** | 16px كحد أدنى |
| **التنقل بلوحة المفاتيح** | Tab للتنقل، Enter للتفعيل |
| **قارئ الشاشة** | aria-label لكل عنصر تفاعلي |
| **النصوص البديلة** | alt لكل صورة |

**مثال:**
```tsx
// ✅ صحيح - زر قابل للوصول
<button
  aria-label="حذف المستخدم أحمد"
  onClick={handleDelete}
  onKeyDown={(e) => e.key === 'Enter' && handleDelete()}
  tabIndex={0}
>
  <TrashIcon aria-hidden="true" />
  <span className="sr-only">حذف</span>
</button>

// ✅ صحيح - نموذج قابل للوصول
<label htmlFor="email">البريد الإلكتروني</label>
<input
  id="email"
  type="email"
  aria-required="true"
  aria-invalid={!!errors.email}
  aria-describedby="email-error"
/>
{errors.email && (
  <span id="email-error" role="alert">
    {errors.email}
  </span>
)}
```

**العقوبة:** تحذير - المكونات غير القابلة للوصول تُطلب تحسينها.

---

## 🔶 فئة: إدارة الحالة والبيانات (State & Data Management)

### القاعدة 39: مصدر الحقيقة الواحد (Single Source of Truth Rule)

> **كل قطعة بيانات يجب أن يكون لها مصدر واحد فقط.**

**المبدأ الأساسي:**
> "لا تكرر البيانات. إذا تغيرت في مكان، يجب أن تتغير في كل مكان."

**أمثلة:**
```typescript
// ❌ خطأ - بيانات مكررة
const [userName, setUserName] = useState('');
const [userEmail, setUserEmail] = useState('');
const [userAge, setUserAge] = useState(0);

// ✅ صحيح - مصدر واحد
const [user, setUser] = useState<User | null>(null);

// ❌ خطأ - حساب مكرر
const [total, setTotal] = useState(0);
const [items, setItems] = useState([]);
// يجب تحديث total يدوياً عند تغيير items

// ✅ صحيح - حساب مشتق
const [items, setItems] = useState([]);
const total = useMemo(
  () => items.reduce((sum, item) => sum + item.price, 0),
  [items]
);
```

**العقوبة:** تحذير - البيانات المكررة تُطلب توحيدها.

---

### القاعدة 40: عدم تغيير البيانات مباشرة (Immutability Rule)

> **البيانات يجب ألا تُغيّر مباشرة. دائماً أنشئ نسخة جديدة.**

**أمثلة:**
```typescript
// ❌ خطأ - تغيير مباشر
const updateUser = (user, name) => {
  user.name = name;  // تغيير مباشر!
  return user;
};

// ✅ صحيح - نسخة جديدة
const updateUser = (user, name) => ({
  ...user,
  name
});

// ❌ خطأ - تغيير مصفوفة مباشرة
const addItem = (items, newItem) => {
  items.push(newItem);  // تغيير مباشر!
  return items;
};

// ✅ صحيح - مصفوفة جديدة
const addItem = (items, newItem) => [...items, newItem];

// ✅ صحيح - حذف من مصفوفة
const removeItem = (items, id) => items.filter(item => item.id !== id);

// ✅ صحيح - تحديث عنصر في مصفوفة
const updateItem = (items, id, updates) => 
  items.map(item => item.id === id ? { ...item, ...updates } : item);
```

**العقوبة:** رفض الدمج - أي mutation مباشر يُرفض.

---

## ⬛ فئة: الأتمتة والـ CI/CD (Automation & CI/CD)

### القاعدة 41: الفحص الآلي قبل الدمج (Pre-merge Checks Rule)

> **لا يُدمج أي كود بدون اجتياز جميع الفحوصات الآلية.**

**الفحوصات المطلوبة:**

```yaml
# .github/workflows/ci.yml
name: CI Pipeline

on: [push, pull_request]

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      
      - name: Install dependencies
        run: pnpm install
      
      - name: Type Check
        run: pnpm run check
      
      - name: Lint
        run: pnpm run lint
      
      - name: Unit Tests
        run: pnpm run test
      
      - name: Build
        run: pnpm run build
      
      - name: Security Audit
        run: pnpm audit
```

**العقوبة:** رفض الدمج تلقائياً - أي فشل في CI يمنع الدمج.

---

### القاعدة 42: رسائل الـ Commit الواضحة (Commit Message Rule)

> **كل commit يجب أن يكون له رسالة واضحة تتبع نمطاً محدداً.**

**نمط الرسالة:**
```
<type>(<scope>): <subject>

<body>

<footer>
```

**أنواع الـ Commit:**

| النوع | الاستخدام |
|:---|:---|
| `feat` | ميزة جديدة |
| `fix` | إصلاح خطأ |
| `docs` | توثيق |
| `style` | تنسيق (لا يغير الكود) |
| `refactor` | إعادة هيكلة |
| `test` | اختبارات |
| `chore` | مهام صيانة |

**أمثلة:**
```bash
# ✅ صحيح
feat(users): إضافة شاشة إدارة المستخدمين
fix(auth): إصلاح مشكلة انتهاء الجلسة
docs(api): توثيق endpoints المستخدمين
refactor(db): تحسين استعلامات الإهلاك

# ❌ خطأ
"تعديلات"
"fix"
"update"
"asdfgh"
```

**العقوبة:** رفض الدمج - رسائل غير واضحة تُرفض.

---


---

## 📊 جدول ملخص القواعد الكاملة (42 قاعدة)

| # | القاعدة | الفئة | العقوبة |
|:---:|:---|:---|:---:|
| 1 | الأسرار الصفرية في الكود | الأمان | رفض |
| 2 | الملفات المحظورة | الأمان | رفض |
| 3 | الحماية الإلزامية (Rate Limiting) | الأمان | رفض |
| 4 | صفر أخطاء TypeScript | الجودة | رفض |
| 5 | نجاح الاختبارات 100% | الجودة | رفض |
| 6 | حد 500 سطر للملف | الجودة | تحذير |
| 7 | منع N+1 Query | الأداء | رفض |
| 8 | تحديد أعمدة SELECT | الأداء | تحذير |
| 9 | فهرسة الأعمدة المستخدمة | الأداء | تحذير |
| 10 | تحسين React (memo, useCallback) | الأداء | تحذير |
| 11 | حذف TODO القديمة | الصيانة | تحذير |
| 12 | منع console.log في الإنتاج | الصيانة | تحذير |
| 13 | تحديث التبعيات الأمنية | الصيانة | إلزامي |
| 14 | الربط الكامل للواجهات | التطوير المتكامل | رفض |
| 15 | البحث والتحقق قبل البناء | التطوير المتكامل | رفض |
| 16 | التوازي الذكي مع الفحص | التطوير المتكامل | رفض |
| 17 | الإصدارات الدلالية | التحكم بالإصدارات | رفض |
| 18 | سجل التغييرات (Changelog) | التحكم بالإصدارات | رفض |
| 19 | منهجية الترحيل والنشر | التحكم بالإصدارات | إعادة |
| 20 | مراجعة الكود | جودة الكود المتقدمة | رفض |
| 21 | تغطية الاختبارات 80% | جودة الكود المتقدمة | رفض |
| 22 | التسمية الموحدة | جودة الكود المتقدمة | رفض |
| 23 | حجم الدوال والملفات | جودة الكود المتقدمة | تحذير |
| 24 | التحقق من المدخلات (Zod) | الأمان المتقدم | رفض |
| 25 | سجلات التدقيق (Audit Logs) | الأمان المتقدم | رفض |
| 26 | عدم كشف الأخطاء الداخلية | الأمان المتقدم | رفض |
| 27 | حماية الـ API الكاملة | الأمان المتقدم | رفض |
| 28 | معالجة الأخطاء الموحدة | إدارة الأخطاء | رفض |
| 29 | التعافي من الأخطاء | إدارة الأخطاء | تحذير |
| 30 | التخزين المؤقت (Caching) | الأداء المتقدم | تحذير |
| 31 | التحميل الكسول (Lazy Loading) | الأداء المتقدم | تحذير |
| 32 | تحسين الاستعلامات | الأداء المتقدم | رفض |
| 33 | توثيق الـ API | التوثيق والصيانة | رفض |
| 34 | التعليقات الذكية | التوثيق والصيانة | تحذير |
| 35 | النسخ الاحتياطي | التوثيق والصيانة | حرج |
| 36 | التغذية الراجعة الفورية | تجربة المستخدم | تحذير |
| 37 | رسائل الخطأ الودية | تجربة المستخدم | تحذير |
| 38 | إمكانية الوصول (Accessibility) | تجربة المستخدم | تحذير |
| 39 | مصدر الحقيقة الواحد | إدارة الحالة | تحذير |
| 40 | عدم تغيير البيانات مباشرة | إدارة الحالة | رفض |
| 41 | الفحص الآلي قبل الدمج (CI) | الأتمتة | رفض تلقائي |
| 42 | رسائل Commit الواضحة | الأتمتة | رفض |

---

## 🎯 مؤشرات الأداء الرئيسية (KPIs)

| المؤشر | الهدف | الحد الأدنى |
|:---|:---:|:---:|
| أخطاء TypeScript | 0 | 0 |
| نسبة نجاح الاختبارات | 100% | 95% |
| تغطية الاختبارات | 90% | 80% |
| ثغرات أمنية حرجة | 0 | 0 |
| ثغرات أمنية متوسطة | 0 | 5 |
| حجم الملف الأقصى | 300 سطر | 500 سطر |
| حجم الدالة الأقصى | 30 سطر | 50 سطر |
| زمن استجابة API | < 200ms | < 500ms |
| وقت تحميل الصفحة | < 2s | < 4s |

---

## 📋 قائمة التحقق الشاملة قبل الدمج

```markdown
## قائمة التحقق - PR #XXX

### الأمان
- [ ] لا يوجد أسرار في الكود
- [ ] المدخلات مُتحقق منها بـ Zod
- [ ] الـ API محمي (Auth + Rate Limit)
- [ ] لا يوجد كشف لأخطاء داخلية

### الجودة
- [ ] صفر أخطاء TypeScript
- [ ] جميع الاختبارات ناجحة
- [ ] تغطية الاختبارات ≥ 80%
- [ ] التسمية موحدة ومتسقة
- [ ] الملفات والدوال ضمن الحدود

### الأداء
- [ ] لا يوجد N+1 Query
- [ ] الاستعلامات محددة الأعمدة
- [ ] Pagination مُطبق
- [ ] Lazy Loading للمكونات الكبيرة

### التوثيق
- [ ] CHANGELOG محدث
- [ ] رقم الإصدار محدث
- [ ] الـ API موثق
- [ ] التعليقات تشرح "لماذا"

### تجربة المستخدم
- [ ] تغذية راجعة فورية
- [ ] رسائل خطأ ودية
- [ ] إمكانية الوصول مُطبقة

### التكامل
- [ ] CRUD مُختبر بالكامل
- [ ] الاتصال بقاعدة البيانات يعمل
- [ ] CI Pipeline ناجح
```

---

**تاريخ آخر تحديث:** 24 ديسمبر 2025
**الإصدار:** 2.0.0
**عدد القواعد:** 42 قاعدة
**الفئات:** 11 فئة

