# سجل التغييرات (Changelog)

جميع التغييرات الملحوظة في هذا المشروع سيتم توثيقها في هذا الملف.

يتبع هذا المشروع نظام [الإصدارات الدلالية](https://semver.org/lang/ar/).

---

## [2.1.0] - 2025-12-25

### ✨ إضافات (Added)
- إضافة نظام Caching متكامل (`server/cache/`)
- إضافة نظام Logging متقدم (`server/logging/`)
- إضافة نظام الإشعارات (`server/notifications/`)
- إضافة نظام رفع الملفات (`server/uploads/`)
- إضافة نظام توليد التقارير (`server/reports/`)
- إضافة قوالب البريد الإلكتروني (`server/email-templates/`)
- إضافة نظام Audit Trail (`server/audit/`)
- إضافة نظام الصلاحيات (`server/permissions/`)
- إضافة نظام الإعدادات (`server/settings/`)
- إضافة Custom Hooks للـ Client (`client/src/hooks/`)
- إضافة مكونات النماذج (`client/src/components/forms/`)
- إضافة مكونات الرسوم البيانية (`client/src/components/charts/`)
- إضافة مكونات الجداول (`client/src/components/tables/`)
- إضافة مكونات النوافذ المنبثقة (`client/src/components/modals/`)
- إضافة مكونات التخطيط (`client/src/components/layout/`)
- إضافة مكونات التنقل (`client/src/components/navigation/`)
- إضافة قوالب الطباعة (`client/src/components/print/`)
- إضافة أدوات التصدير (`client/src/utils/export/`)
- إضافة نظام الترجمة i18n (`client/src/i18n/`)
- إضافة CI/CD Pipeline (`.github/workflows/ci.yml`)
- إكمال 30 مهمة تطوير

### 🔧 تعديلات (Changed)
- تحسين أداء قاعدة البيانات (`server/database/`)
- تحديث أنواع TypeScript في مكونات الجداول
- تحسين معالجة الأخطاء في جميع الملفات

### 🐛 إصلاحات (Fixed)
- إصلاح جميع أخطاء TypeScript (53 خطأ)
- إصلاح أخطاء في صفحات fieldops
- إصلاح أخطاء في صفحات diesel
- إصلاح أخطاء في صفحات accounting
- إصلاح أخطاء في صفحات assets

### 🔒 أمان (Security)
- تحديث الحزم لإصلاح 6 ثغرات متوسطة

---

## [2.0.0] - 2025-12-24

### ✨ إضافات (Added)
- إضافة ملف القواعد الصارمة (42 قاعدة) للتطوير والصيانة
- إضافة نظام Logger متكامل (`server/logger.ts`)
- إضافة Rate Limiting للحماية من هجمات DDoS
- إضافة Helmet لرؤوس الأمان
- إضافة ملف CHANGELOG.md لتوثيق التغييرات
- إضافة إعدادات ESLint للجودة
- إضافة GitHub Actions للـ CI/CD
- إضافة قوالب Pull Request و Issues

### 🔧 تعديلات (Changed)
- تحسين هيكل المشروع وتقسيم الملفات الكبيرة
- تحديث إعدادات TypeScript
- تحسين أداء استعلامات قاعدة البيانات

### 🐛 إصلاحات (Fixed)
- إصلاح جميع أخطاء TypeScript (468 خطأ)
- إصلاح مشاكل الأمان المكتشفة
- حذف الملفات الحساسة من المستودع

### 🔒 أمان (Security)
- حذف كلمات المرور الثابتة من الكود
- حذف ملفات النسخ الاحتياطي الحساسة
- إضافة حماية Rate Limiting
- إضافة رؤوس أمان HTTP

---

## [1.0.0] - 2025-12-01

### ✨ إضافات (Added)
- الإصدار الأولي لنظام إدارة الطاقة المتكامل
- إدارة المستخدمين والصلاحيات
- إدارة الأصول والإهلاك
- إدارة الفواتير والمدفوعات
- لوحة التحكم والتقارير
- نظام المصادقة والتفويض

---

## قواعد كتابة سجل التغييرات

### أنواع التغييرات
- **✨ إضافات (Added)**: ميزات جديدة
- **🔧 تعديلات (Changed)**: تغييرات في الوظائف الحالية
- **⚠️ مهمل (Deprecated)**: ميزات ستُزال قريباً
- **🗑️ حذف (Removed)**: ميزات تم حذفها
- **🐛 إصلاحات (Fixed)**: إصلاح الأخطاء
- **🔒 أمان (Security)**: تحديثات أمنية

### قواعد الكتابة
1. أحدث الإصدارات في الأعلى
2. كل إصدار يحتوي على التاريخ
3. التغييرات مُصنفة حسب النوع
4. وصف واضح ومختصر لكل تغيير
