# استيراد النسخة الاحتياطية لقاعدة البيانات

## ✅ ما تم إنجازه

1. ✅ تم إنشاء قاعدة البيانات `666666` بنجاح
2. ✅ تم تحديث ملف `.env` للاتصال بقاعدة البيانات الجديدة
3. ✅ تم إزالة DEMO_MODE من ملف `.env`

## 📋 معلومات قاعدة البيانات

- **اسم القاعدة**: `666666`
- **الخادم**: localhost:3306
- **المستخدم**: root
- **الترميز**: utf8mb4_unicode_ci

## 📥 استيراد النسخة الاحتياطية

عندما ترسل النسخة الاحتياطية، يمكن استيرادها بإحدى الطرق التالية:

### الطريقة 1: استخدام MySQL Command Line

```powershell
# إذا كانت النسخة الاحتياطية ملف .sql
& "C:\xampp\mysql\bin\mysql.exe" -u root 666666 < "path\to\backup.sql"

# أو باستخدام PowerShell
Get-Content "path\to\backup.sql" | & "C:\xampp\mysql\bin\mysql.exe" -u root 666666
```

### الطريقة 2: استخدام phpMyAdmin

1. افتح phpMyAdmin: http://localhost/phpmyadmin
2. اختر قاعدة البيانات `666666` من القائمة الجانبية
3. انقر على تبويب "Import"
4. اختر ملف النسخة الاحتياطية
5. انقر على "Go" لبدء الاستيراد

### الطريقة 3: استخدام MySQL Workbench

1. افتح MySQL Workbench
2. اتصل بالخادم المحلي
3. انقر على "Server" > "Data Import"
4. اختر "Import from Self-Contained File"
5. اختر ملف النسخة الاحتياطية
6. اختر قاعدة البيانات `666666` كهدف
7. انقر على "Start Import"

## 🔍 التحقق من الاستيراد

بعد الاستيراد، تحقق من:

```powershell
# عرض جميع الجداول
& "C:\xampp\mysql\bin\mysql.exe" -u root 666666 -e "SHOW TABLES;"

# عرض عدد الجداول
& "C:\xampp\mysql\bin\mysql.exe" -u root 666666 -e "SELECT COUNT(*) as table_count FROM information_schema.tables WHERE table_schema = '666666';"
```

## ⚙️ بعد الاستيراد

بعد استيراد النسخة الاحتياطية بنجاح:

1. **تأكد من تحديث ملف `.env`**:
   ```env
   DATABASE_URL=mysql://root@localhost:3306/666666
   ```

2. **شغّل Database Migration** (إذا لزم الأمر):
   ```powershell
   pnpm db:push
   ```

3. **أعد تشغيل الخادم**:
   ```powershell
   pnpm dev
   ```

## 📝 ملاحظات

- تأكد من أن MySQL في XAMPP يعمل قبل الاستيراد
- إذا كانت النسخة الاحتياطية كبيرة، قد يستغرق الاستيراد بعض الوقت
- تأكد من أن الترميز (charset) متوافق (utf8mb4)

---

**جاهز لاستقبال النسخة الاحتياطية!** 📦

