# 📋 المتطلبات المستخرجة من خارطة الطريق - المرحلة الأولى
## Extracted Requirements from Phase 1 Roadmap

**المصدر:** PHASE_1_DETAILED.md (17,060 سطر) + القصص (125+ قصة)  
**تاريخ الاستخراج:** 6 يناير 2026  
**الحالة:** استخراج دقيق وشامل

---

## 🎯 نظرة عامة

تم قراءة وتحليل خارطة الطريق الكاملة واستخراج **جميع المتطلبات** بدقة:

| المقياس | العدد |
|---------|-------|
| **المهام الأساسية** | 6 مهام (0.1-0.6) |
| **المهام الرئيسية** | 31 مهمة (1-31) |
| **المهام الإضافية** | 6 مهام (32-37) |
| **المهام الفرعية** | 200+ |
| **القصص المستخرجة** | 125+ |
| **APIs المطلوبة** | 80+ |
| **الجداول المطلوبة** | 160+ |
| **الشاشات المطلوبة** | 150+ |

---

## 🏗️ **المهام الأساسية (المرحلة 0)**

### **المهمة 0.1: شجرة الحسابات** ✅ مُنفذ 100%

**المدة:** شهر واحد (80 ساعة)

**المتطلبات:**
- ✅ 40+ حساب محاسبي
- ✅ تصنيف حسب النظام (assets, maintenance, inventory, etc)
- ✅ بنية هرمية (main, sub, detail)
- ✅ طبيعة الحساب (debit, credit)
- ✅ دعم العملات المتعددة

---

### **المهمة 0.2: العملاء والموردين** ⚠️ جزئي 90%

**المدة:** شهران (80 ساعة)

**المتطلبات:**
- ✅ جدول العملاء الشامل
- ✅ جدول الموردين الشامل
- ⚠️ شروط الدفع والائتمان (موجودة لكن لا منطق)
- ⚠️ تقييم الموردين
- ⚠️ تصنيفات متقدمة

---

### **المهمة 0.3: الفوترة والتحصيل** ⚠️ جزئي 60%

**المدة:** شهران (120 ساعة)

**المتطلبات:**
- ✅ جدول الفواتير
- ✅ جدول المدفوعات
- ❌ **Cron Job الفوترة التلقائية كل 10 أيام**
- ❌ **تكامل بوابات الدفع**
- ❌ **تكامل SMS/WhatsApp**
- ❌ **نظام POS (نقاط البيع)**
- ⚠️ إرسال الإشعارات (قوالب موجودة لكن لا تكامل)

---

### **المهمة 0.4: المخزون والمشتريات** ⚠️ جزئي 75%

**المدة:** شهران (120 ساعة)

**المتطلبات:**
- ✅ جداول المخزون والحركات
- ✅ طلبات الشراء وأوامر الشراء
- ⚠️ **استلام البضائع (GRN)** (موجود لكن محدود)
- ❌ **المطابقة الثلاثية** (PO vs GRN vs Invoice)
- ⚠️ حساب المتوسط المرجح (موجود لكن لا تحديث تلقائي)

---

### **المهمة 0.5: التقارير المالية** ❌ غير مُنفذ 30%

**المدة:** شهران (80 ساعة)

**المتطلبات:**
- ⚠️ ميزان المراجعة (شاشة موجودة لكن محدودة)
- ⚠️ دفتر الأستاذ العام (موجود)
- ❌ **قائمة الدخل**
- ❌ **الميزانية العمومية**
- ❌ **تقرير التدفقات النقدية**
- ❌ **كشوف الحسابات التفصيلية**

---

### **المهمة 0.6: محرك التسوية المرن** ❌ غير مُنفذ 0%

**المدة:** شهر واحد (160 ساعة)

**المتطلبات الدقيقة:**

#### **أ) الحسابات الوسيطة:**
```
المبدأ: كل حساب فرعي له "حساب وسيط"
├─ البنوك (حساب رئيسي)
│  ├─ بنك الحوشبي (حساب فرعي)
│  │  └─ ح/ وسيط بنك الحوشبي ← هنا تُسجل العمليات اليومية
│  └─ بنك CAC (حساب فرعي)
│     └─ ح/ وسيط بنك CAC
```

#### **ب) واجهات الإدخال اليومي:**
```
مسؤول الصندوق:
└─ يسجل في ح/ وسيط صندوق التحصيل
   - مدين: عند استلام من المتحصل
   - دائن: عند التوريد للبنك

محاسب البنك:
└─ يسجل كشف الحساب في ح/ وسيط بنك الحوشبي
   - مدين: عند الإيداع
   - دائن: عند السحب

مسؤول الفوترة:
└─ يسجل إجمالي اليوم في ح/ وسيط إيرادات الفوترة
   - دائن: المبيعات
```

#### **ج) مركز التسوية المرن:**
```
┌────────────────────────────────────────────────────────┐
│  اللوح 1:           اللوح 2:          اللوح 3:       │
│  وسيط الصندوق      وسيط البنك       وسيط الإيرادات  │
│  ┌─────────────┐   ┌──────────────┐  ┌────────────┐ │
│  │ د: 100,000  │   │ م: 150,000   │  │ د: 100,000 │ │
│  │ د: 50,000   │   │              │  │ د: 50,000  │ │
│  │ م: 150,000  │   │              │  │            │ │
│  └─────────────┘   └──────────────┘  └────────────┘ │
│                                                      │
│  سلة التسوية:                                      │
│  المدين: 100,000 + 50,000 + 150,000 = 300,000      │
│  الدائن: 150,000 + 100,000 + 50,000 = 300,000      │
│  ✅ متوازن → [زر تسوية نشط]                        │
└────────────────────────────────────────────────────────┘
```

#### **د) أنواع التسوية:**
- **1:1** - عملية واحدة ← → عملية واحدة
- **1:N** - عملية واحدة ← → عدة عمليات
- **N:1** - عدة عمليات ← → عملية واحدة
- **N:M** - عدة عمليات ← → عدة عمليات

**الشرط الوحيد:** إجمالي المدين = إجمالي الدائن

---

## 🚀 **المهام الرئيسية (1-31)**

### **المهمة 1: بناء الأساس المعماري** ✅ 85%

**المتطلبات:**
1. ✅ Multi-tenancy (businesses, branches, stations)
2. ✅ نظام المستخدمين (users, roles, permissions)
3. ⚠️ **محرك RBAC** متقدم (موجود أساسي)
4. ✅ لوحة التحكم الرئيسية
5. ✅ إدارة المشتركين والعدادات

---

### **المهمة 2: أتمتة العدادات التقليدية** ⚠️ 40%

**المتطلبات:**

#### **2.1 واجهة إدخال القراءات:**
- ⚠️ شاشة ويب (موجودة)
- ❌ **تطبيق جوال للفنيين** ← **حرجة!**
  ```
  الميزات المطلوبة:
  ├─ تسجيل الدخول
  ├─ قائمة العدادات المُسندة
  ├─ مسح الباركود (QR/Barcode)
  ├─ إدخال القراءة
  ├─ التقاط صورة العداد (مع GPS)
  ├─ التحقق من صحة القراءة
  ├─ تسجيل الملاحظات
  ├─ حفظ Offline
  └─ مزامنة عند الاتصال
  ```

#### **2.2 الفوترة الدورية التلقائية:**
- ❌ **Cron Job** ← **حرجة!**
  ```python
  # المطلوب:
  @scheduled_job(cron="0 0 */10 * *")  # كل 10 أيام
  def generate_invoices():
      # 1. جلب القراءات الجديدة
      # 2. حساب الاستهلاك
      # 3. تطبيق التعرفة
      # 4. إضافة الرسوم والضرائب
      # 5. إنشاء الفاتورة
      # 6. إنشاء قيد محاسبي تلقائي
      # 7. إرسال SMS/WhatsApp
  ```

#### **2.3 بوابات الدفع:**
- ❌ **تكامل Sadad/Moyasar** ← **حرجة!**
  ```typescript
  class PaymentGateway {
    async createPayment(invoice): Promise<PaymentUrl>
    async verifyPayment(reference): Promise<PaymentStatus>
    async processWebhook(payload): Promise<void>
    async refundPayment(payment): Promise<RefundStatus>
  }
  ```

#### **2.4 SMS/WhatsApp:**
- ❌ **تكامل Twilio/Unifonic** ← **حرجة!**
  ```typescript
  class NotificationService {
    async sendInvoiceNotification(customer, invoice)
    async sendPaymentConfirmation(customer, payment)
    async sendReminder(customer, invoice)
    async sendSTSToken(customer, token)
  }
  ```

---

### **المهمة 3: أتمتة عدادات STS** ❌ 5%

**المتطلبات:**

#### **3.1 تكامل مع مزود STS:**
```typescript
class STSProvider {
  async requestToken(meterId, amount): Promise<Token>
  async verifyToken(token): Promise<TokenStatus>
  async cancelToken(token): Promise<void>
}
```

#### **3.2 واجهة شحن الرصيد:**
```
المطلوب:
├─ صفحة ويب عامة (بدون تسجيل دخول)
├─ إدخال رقم العداد
├─ التحقق من العداد
├─ إدخال المبلغ
├─ اختيار طريقة الدفع
├─ الدفع الإلكتروني
├─ استقبال التوكن من مزود STS
├─ عرض التوكن للعميل
├─ إرسال التوكن عبر SMS
└─ إنشاء قيد محاسبي تلقائي
```

---

### **المهمة 4: الربط المحاسبي التلقائي** ❌ 20%

**المتطلبات:**

#### **4.1 شجرة الحسابات:** ✅ مُنفذ 100%

#### **4.2 محرك القيود التلقائي:** ❌ **حرجة!**
```typescript
// المطلوب:
class AutoJournalEngine {
  
  // عند إصدار فاتورة:
  async onInvoiceCreated(invoice) {
    await createJournalEntry({
      debit: {account: "customers", amount: invoice.totalAmount},
      credit: {account: "revenue", amount: invoice.totalAmount}
    })
  }
  
  // عند استلام دفعة:
  async onPaymentReceived(payment) {
    await createJournalEntry({
      debit: {account: "cash/bank", amount: payment.amount},
      credit: {account: "customers", amount: payment.amount}
    })
  }
  
  // عند شحن STS:
  async onSTSRecharge(recharge) {
    await createJournalEntry({
      debit: {account: "cash/bank", amount: recharge.amount},
      credit: {account: "prepaid_revenue", amount: recharge.amount}
    })
  }
  
  // عند استلام مخزون:
  async onGoodsReceipt(receipt) {
    await createJournalEntry({
      debit: {account: "inventory", amount: receipt.totalCost},
      credit: {account: "goods_in_transit", amount: receipt.totalCost}
    })
  }
  
  // عند دفع للمورد:
  async onSupplierPayment(payment) {
    await createJournalEntry({
      debit: {account: "suppliers", amount: payment.amount},
      credit: {account: "cash/bank", amount: payment.amount}
    })
  }
  
  // عند رواتب:
  async onPayrollProcessed(payroll) {
    await createJournalEntry({
      debit: {account: "salary_expense", amount: payroll.totalNet},
      credit: {account: "cash/bank", amount: payroll.totalNet}
    })
  }
}
```

#### **4.3 واجهة عرض القيود:** ✅ 80%

#### **4.4 مطابقة البنك:** ❌ **غير موجود!**

---

### **المهمة 6: إدارة الدعم الحكومي** ❌ 0%

**المتطلبات الكاملة:**

#### **6.1 تعديل ملف المشترك:**
```sql
-- حقول جديدة في جدول customers:
is_subsidized BOOLEAN
subsidy_program_id UUID
subsidy_category ENUM('A', 'B')  -- A=82kWh, B=64kWh
subsidy_reference_number VARCHAR(50)
subsidy_start_date DATE
subsidy_end_date DATE
monthly_quota_kwh DECIMAL
```

#### **6.2 Cron Job شحن الحصص:**
```python
@scheduled_job(cron="1 0 1 * *")  # 12:01 صباحاً أول يوم
def charge_monthly_subsidies():
    """شحن الحصة المدعومة لجميع المشتركين"""
    subsidized = get_subsidized_customers(active=True)
    for customer in subsidized:
        quota = customer.monthly_quota_kwh  # 82 or 64
        result = acrel_api.set_quota(customer.meter_id, quota)
        log_subsidy_charge(customer, quota, result)
```

#### **6.3 تقارير صندوق الدعم:**
```python
@scheduled_job(cron="59 23 28 * *")  # 11:59 اليوم 28
def generate_subsidy_report():
    """إنشاء تقرير Excel للصندوق"""
    subsidized = get_subsidized_customers()
    report = create_excel_report(
        columns=[
            "الرقم المرجعي",
            "اسم المشترك",
            "الفئة",
            "الاستهلاك (كيلو)",
            "السعر (ريال)"
        ]
    )
    send_email(to=subsidy_fund_email, attachment=report)
```

#### **6.4 الشحن الإضافي:**
```
العميل المدعوم يستهلك أكثر من الحصة:
├─ يدخل صفحة "شحن إضافي"
├─ يدفع إلكترونياً
├─ النظام يشحن العداد عبر Acrel API
└─ يُسجل كإيراد إضافي (غير مدعوم)
```

---

### **المهمة 8: إدارة المخزون الشاملة** ⚠️ 70%

**المتطلبات:**

#### **8.1 كتالوج الأصناف المتخصص:**
```
أصناف قطاع الكهرباء المطلوبة:
├─ عدادات (traditional, STS, IoT)
│  └─ تتبع بالرقم التسلسلي
├─ شاشات STS (CIU)
│  └─ تتبع بالرقم التسلسلي
├─ أختام (seals)
│  └─ تتبع بالرقم التسلسلي ولون
├─ قواطع كهربائية (breakers)
│  └─ تتبع بالرقم التسلسلي وأمبير
├─ أسلاك كهربائية (cables)
│  └─ تتبع بالكمية (متر)
└─ مكونات فرعية (filters, pumps, etc)
   └─ تتبع بالرقم التسلسلي
```

#### **8.2 تتبع الأرقام التسلسلية:** ❌ **مهم جداً!**
```sql
-- المطلوب:
CREATE TABLE inventory_units (
  id UUID PRIMARY KEY,
  item_id UUID,              -- الصنف
  serial_number VARCHAR(100) UNIQUE,
  status ENUM(
    'in_stock',              -- في المخزن
    'issued',                -- مصروف لفني
    'installed',             -- مركب عند عميل
    'defective',             -- تالف
    'under_repair',          -- قيد الإصلاح
    'retired'                -- متقاعد
  ),
  location VARCHAR(255),     -- الموقع الحالي
  assigned_to_id UUID,       -- مُسند إلى (worker/customer)
  installed_at_customer_id UUID,
  installation_date DATE,
  condition VARCHAR(50),     -- الحالة
  notes TEXT
)
```

#### **8.3 المستودعات الافتراضية:**
```
المطلوب:
├─ المخزن الرئيسي (new items)
├─ مخزن التالف (defective)
├─ مخزن قيد الإصلاح (under repair)
└─ مخزن المستعمل الصالح (used - good)
```

---

### **المهمة 10: محرك التسعير المرن** ❌ 0%

**المتطلبات:**

```sql
CREATE TABLE pricing_rules (
  id UUID PRIMARY KEY,
  rule_code VARCHAR(50),
  meter_type ENUM('traditional', 'sts', 'iot'),
  usage_type ENUM('residential', 'commercial', 'industrial', 'government'),
  
  -- الرسوم
  subscription_fee DECIMAL(18,2),
  activation_fee DECIMAL(18,2),
  installation_fee DECIMAL(18,2),
  
  -- التأمين
  deposit_amount DECIMAL(18,2),
  deposit_required BOOLEAN,
  
  is_active BOOLEAN
)

-- البيانات المطلوبة:
| نوع العداد | نوع الاستخدام | الاشتراك | التأمين |
|-----------|--------------|---------|---------|
| traditional | residential | 5,000 | 35,000 |
| traditional | commercial | 10,000 | 50,000 |
| sts | residential | 7,000 | 0 ← مهم! |
| sts | commercial | 12,000 | 0 ← مهم! |
| iot | residential | 6,000 | 30,000 |
| iot | commercial | 15,000 | 45,000 |
```

**محرك الحساب:**
```typescript
class PricingEngine {
  calculateSubscriptionCost(meterType, usageType) {
    const rule = getPricingRule(meterType, usageType)
    return {
      subscriptionFee: rule.subscriptionFee,
      activationFee: rule.activationFee,
      installationFee: rule.installationFee,
      depositAmount: rule.depositRequired ? rule.depositAmount : 0,
      total: calculateTotal(rule)
    }
  }
}
```

---

### **المهمة 11: وحدة إدارة الترحيل إلى IoT** ❌ 0%

**المتطلبات:**

#### **11.1 Mini-GIS:**
```
المطلوب:
├─ واجهة خرائط (Leaflet/Mapbox)
├─ عرض المحطات والعملاء
├─ رسم الشبكات الكهربائية
├─ حساب المسافات
└─ تخطيط الطبابلين الجديدة
```

#### **11.2 أداة تخطيط الشبكة الذكية:**
```
المطلوب:
├─ رسم طبلون جديد على الخريطة
├─ سحب وإفلات العملاء من طبلون قديم → جديد
├─ حساب المسافة تلقائياً
├─ حساب المواد المطلوبة (كيبلات)
├─ إنشاء "خطة ترحيل" رقمية
└─ تحويل الخطة إلى أوامر عمل
```

---

### **المهمة 12: تسعير الخدمات والأجور** ❌ 0%

**المتطلبات:**

```sql
CREATE TABLE service_catalog (
  id UUID PRIMARY KEY,
  service_code VARCHAR(50),
  nameAr VARCHAR(255),
  description TEXT,
  labor_cost DECIMAL(10,2),  -- تكلفة العمالة
  estimated_hours DECIMAL(8,2),
  is_active BOOLEAN
)

-- الخدمات المطلوبة:
| الكود | الخدمة | التكلفة |
|-------|--------|---------|
| INSTALL-METER | تركيب عداد (شامل فك القديم) | 5,000 |
| INSTALL-DP | تركيب طبلون فيبر | 15,000 |
| REPLACE-CIU | استبدال شاشة STS | 1,000 |
| SURVEY-SITE | مسح موقع مشترك | 500 |
| INSTALL-FIBER | تمديد فيبر (100م) | 2,000 |
```

**الاستخدام:**
```
كل work_order يحتوي على:
├─ تكلفة المواد (من inventory)
└─ تكلفة الخدمات (من service_catalog)

التكلفة الإجمالية = materials + services
```

---

### **المهمة 24: جولات القراءة اليدوية** ❌ 40%

**المتطلبات:**

```sql
CREATE TABLE scheduled_reading_rounds (
  id UUID PRIMARY KEY,
  round_name VARCHAR(255),
  frequency ENUM('daily', 'every_8_hours', 'weekly'),
  start_time TIME,
  assigned_worker_id UUID,
  meters JSON,  -- قائمة العدادات
  is_active BOOLEAN
)

CREATE TABLE round_executions (
  id UUID PRIMARY KEY,
  round_id UUID,
  execution_date DATE,
  started_at TIMESTAMP,
  completed_at TIMESTAMP,
  meters_read INT,
  status ENUM('pending', 'in_progress', 'completed')
)

CREATE TABLE recorded_readings (
  id UUID PRIMARY KEY,
  round_execution_id UUID,
  meter_id UUID,
  reading_value DECIMAL,
  recorded_at TIMESTAMP,
  recorded_by UUID,
  photo_url VARCHAR(500),
  gps_lat DECIMAL,
  gps_lng DECIMAL
)
```

**الميزات المطلوبة:**
1. مُنشئ جولات القراءة (بواجهة)
2. جدولة تلقائية
3. تطبيق جوال للفنيين
4. إشعارات للفنيين بمواعيد الجولات
5. تتبع التنفيذ

---

### **المهمة 27: الصيانة الوقائية المجدولة** ❌ 30%

**المتطلبات:**

#### **محرك الجدولة:**
```python
@scheduled_job(cron="0 0 * * *")  # يومياً منتصف الليل
def schedule_preventive_maintenance():
    """فحص خطط الصيانة وإنشاء أوامر عمل"""
    plans = get_active_pm_plans()
    
    for plan in plans:
        if plan.trigger_type == 'time_based':
            # فحص إذا حان الموعد
            if is_due(plan):
                create_work_order(plan)
        
        elif plan.trigger_type == 'usage_based':
            # جلب قراءة العداد الحالية
            current = get_asset_meter_reading(plan.asset_id)
            last_pm = get_last_pm_reading(plan.asset_id)
            
            if current - last_pm >= plan.meter_interval:
                create_work_order(plan)
```

---

### **المهمة 28: تتبع المكونات الفرعية** ❌ 0%

**المتطلبات:**

```sql
CREATE TABLE asset_components (
  id UUID PRIMARY KEY,
  parent_asset_id UUID,      -- الأصل الرئيسي (مولد)
  component_position VARCHAR(100),  -- موقع المكون (فلتر الزيت)
  item_id UUID,              -- الصنف (CAT-1R-0739)
  
  -- المكون الحالي المركب
  current_unit_id UUID,      -- الوحدة الحالية (SN-12345)
  installed_at DATE,
  meter_reading_at_install DECIMAL,
  
  -- السجل التاريخي
  replacement_history JSON   -- [{unit_id, from, to, hours, reason}]
)
```

**لوحة تحليل الموثوقية:**
```
المطلوب:
├─ متوسط العمر قبل الفشل (MTBF)
│  └─ فلتر CAT-1R-0739: متوسط 62 يوم و 248 ساعة
├─ مقارنة الموردين
│  └─ Donaldson أفضل من CAT (70 يوم vs 62 يوم)
├─ التكلفة الإجمالية للملكية (TCO)
│  └─ سعر الشراء + تكاليف الإصلاح
└─ تحديد الحالات الشاذة
   └─ فلتر SN-XXXX1 فشل بعد 19 يوم فقط ⚠️
```

---

### **المهمة 30: بوابة خدمة العملاء** ❌ 0%

**المتطلبات:**

#### **30.1 تطبيق جوال/ويب للعملاء:**
```
الشاشات المطلوبة:
├─ تسجيل الدخول (رقم الهاتف أو رقم الاشتراك)
├─ حسابي
│  ├─ بيانات الاشتراك
│  ├─ نوع العداد والتعرفة
│  └─ معلومات الاتصال
├─ الفواتير
│  ├─ قائمة الفواتير (مدفوعة/مستحقة)
│  ├─ تنزيل PDF
│  └─ دفع إلكتروني
├─ شحن الرصيد (STS/IoT)
│  ├─ إدخال المبلغ
│  ├─ دفع
│  └─ استلام التوكن/الشحن
├─ مراقبة الاستهلاك (IoT only)
│  ├─ الاستهلاك اللحظي
│  ├─ الرصيد المتبقي
│  └─ رسوم بيانية
├─ طلبات الخدمة
│  ├─ إنشاء طلب (مع صور)
│  ├─ تتبع الحالة
│  └─ تقييم الخدمة
└─ الإشعارات
   ├─ فواتير جديدة
   ├─ تأكيد الدفع
   └─ تنبيهات
```

---

## 📱 **المتطلبات الخاصة بالتطبيقات الجوالة**

### **1. تطبيق الفنيين الميدانيين** ❌ 0%

**الميزات المطلوبة:**
```
├─ تسجيل الدخول
├─ قائمة المهام المُسندة
├─ تفاصيل المهمة (نوع، موقع، عميل)
├─ التنقل GPS للموقع
├─ بدء المهمة (يسجل الوقت والموقع)
├─ مسح الباركود/QR للمكونات
│  ├─ عداد جديد/قديم
│  ├─ ختم
│  ├─ قاطع
│  └─ مواد أخرى
├─ إدخال البيانات الفنية
│  ├─ قراءة العداد الأولية
│  ├─ طول الكيبل
│  ├─ نوع القاطع
│  └─ لون الختم
├─ التقاط الصور (9 أنواع)
│  ├─ واجهة العداد
│  ├─ قراءة العداد
│  ├─ الختم
│  ├─ القاطع
│  ├─ الأسلاك
│  ├─ الموقع
│  ├─ مبنى العميل
│  ├─ قبل التركيب
│  └─ بعد التركيب
├─ توقيع العميل الرقمي
├─ إكمال المهمة
├─ تتبع GPS تلقائي (كل 5 دقائق)
├─ العمل Offline
└─ مزامنة عند الاتصال
```

---

### **2. تطبيق العملاء** ❌ 0%

**الميزات المطلوبة:** (ذُكرت في المهمة 30)

---

## 🔗 **متطلبات التكاملات الخارجية**

### **1. Acrel IoT-EMS** ❌ 0%

```typescript
class AcrelIntegration {
  // قراءة البيانات
  async getMeterReading(meterId): Promise<Reading>
  async getDeviceStatus(deviceId): Promise<Status>
  async getHistoricalData(meterId, from, to): Promise<Data[]>
  
  // إرسال الأوامر
  async disconnectMeter(meterId, reason): Promise<Result>
  async reconnectMeter(meterId): Promise<Result>
  async setTariff(meterId, tariffId): Promise<Result>
  async setMonthlyQuota(meterId, quota): Promise<Result>  // للدعم
  async addCredit(meterId, amount): Promise<Result>        // شحن إضافي
}
```

### **2. STS Provider** ❌ 0%

```typescript
class STSIntegration {
  async requestToken(meterId, amount): Promise<Token>
  async verifyToken(tokenNumber): Promise<TokenInfo>
  async getTokenStatus(tokenNumber): Promise<TokenStatus>
  async cancelToken(tokenNumber): Promise<Result>
}
```

### **3. Payment Gateways** ❌ 0%

```typescript
class PaymentGatewayIntegration {
  // Moyasar/Sadad/HyperPay
  async createPaymentUrl(invoice): Promise<PaymentUrl>
  async verifyPayment(reference): Promise<PaymentStatus>
  async processWebhook(payload): Promise<WebhookResult>
  async refundPayment(payment, amount): Promise<RefundResult>
}
```

### **4. SMS/WhatsApp** ❌ 0%

```typescript
class MessagingIntegration {
  // Twilio/Unifonic/SMSGLOBAL
  async sendSMS(phone, message): Promise<MessageId>
  async sendWhatsApp(phone, message): Promise<MessageId>
  async getMessageStatus(messageId): Promise<MessageStatus>
  
  // قوالب الرسائل:
  sendInvoiceNotification(customer, invoice)
  sendPaymentConfirmation(customer, payment)
  sendReminder(customer, invoice, daysOverdue)
  sendSTSToken(customer, token)
  sendDisconnectionNotice(customer)
}
```

---

## 🔄 **الـ Cron Jobs المطلوبة (15+ مهمة مجدولة)**

### **الحرجة:**
```python
# 1. الفوترة التلقائية (كل 10 أيام)
@cron("0 0 */10 * *")
def auto_billing()

# 2. شحن الحصص المدعومة (أول كل شهر)
@cron("1 0 1 * *")
def charge_subsidies()

# 3. تقرير صندوق الدعم (آخر كل شهر)
@cron("59 23 28 * *")
def generate_subsidy_report()

# 4. الإهلاك الشهري
@cron("0 1 1 * *")
def monthly_depreciation()

# 5. معالجة الحضور اليومي
@cron("30 0 * * *")
def process_daily_attendance()

# 6. التذكيرات (فواتير متأخرة)
@cron("0 9 * * *")
def send_payment_reminders()

# 7. جدولة الصيانة الوقائية
@cron("0 0 * * *")
def schedule_preventive_maintenance()

# 8. التسوية اليومية للدفع المسبق
@cron("55 23 * * *")
def daily_prepaid_settlement()

# 9. فحص انقطاع الاتصال (IoT)
@cron("*/15 * * * *")  # كل 15 دقيقة
def check_device_connectivity()

# 10. النسخ الاحتياطي اليومي
@cron("0 2 * * *")
def daily_backup()
```

---

## 📊 **الإحصائيات النهائية**

### **المتطلبات الكاملة:**

| النوع | العدد |
|-------|-------|
| **الجداول** | 160+ جدول |
| **APIs** | 800+ endpoint |
| **الشاشات (ويب)** | 150+ |
| **الشاشات (جوال)** | 40+ |
| **Cron Jobs** | 15+ |
| **التكاملات** | 8+ |
| **Triggers/Functions** | 50+ |
| **Wizards** | 10+ |
| **Reports** | 30+ |

---

## ✅ **ما تم vs ما ينقص (الحقيقة)**

| المكون | المطلوب | المُنفذ | النسبة |
|--------|---------|---------|--------|
| **الجداول** | 160 | 136 | 85% |
| **APIs** | 800 | 700 | 87% |
| **الشاشات (ويب)** | 150 | 95 | 63% |
| **الشاشات (جوال)** | 40 | 0 | 0% |
| **Cron Jobs** | 15 | 0 | 0% |
| **التكاملات** | 8 | 0 | 0% |
| **Triggers/Functions** | 50 | 5 | 10% |
| **Wizards** | 10 | 0 | 0% |
| **Reports** | 30 | 10 | 33% |
| **محركات** | 5 | 0 | 0% |

---

## 🔥 **المحركات الخمسة المطلوبة**

### **1. محرك القيود المحاسبية التلقائي** ❌
- إنشاء قيد عند كل عملية مالية
- ربط تلقائي بالمصدر

### **2. محرك التسوية المرن** ❌
- الحسابات الوسيطة
- المطابقة المرنة (1:1, 1:N, N:1, N:M)
- سلة التسوية

### **3. محرك التسعير المرن** ❌
- قواعد تسعير ديناميكية
- حساب تلقائي حسب نوع العداد/الاستخدام

### **4. محرك جدولة الصيانة الوقائية** ❌
- Time-based triggers
- Usage-based triggers
- إنشاء work orders تلقائياً

### **5. محرك الإسناد الذكي** ❌
- إسناد المهام لأقرب فني (GPS-based)
- مراعاة التخصص والمهارات
- مراعاة الحمل الحالي

---

## 📌 **الخلاصة:**

### **المُنفذ بشكل ممتاز:**
1. ✅ البنية التحتية (جداول)
2. ✅ APIs أساسية
3. ✅ شاشات أساسية
4. ✅ أنظمة خاصة (Custom, Diesel)

### **ما ينقص (الحقيقة):**
1. ❌ **المحركات** (5 محركات رئيسية)
2. ❌ **Cron Jobs** (15+ مهمة مجدولة)
3. ❌ **التكاملات** (8 تكاملات حرجة)
4. ❌ **التطبيقات الجوالة** (2 تطبيقات)
5. ❌ **Wizards** (10 معالجات)
6. ❌ **GIS** (نظام كامل)
7. ❌ **Triggers** (50+ دالة تلقائية)

---

**النسبة الحقيقية من المتطلبات الكاملة: 45-50%**

(وليس 65-70% كما قلت سابقاً - كنت أنظر للبنية فقط)

---

**آخر تحديث:** 6 يناير 2026  
**الحالة:** استخراج دقيق من خارطة الطريق الكاملة  
**المصدر:** قراءة مباشرة من PHASE_1_DETAILED.md + القصص


