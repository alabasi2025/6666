# تحميل النسخة الاحتياطية من GitHub

## 📥 الطريقة 1: التحميل المباشر من GitHub

1. افتح المتصفح وانتقل إلى:
   ```
   https://github.com/alabasi2025/6666/blob/main/db/backup-utf8.zip
   ```

2. انقر على زر "Download" أو "Raw" لتحميل الملف

3. احفظ الملف في المجلد التالي:
   ```
   F:\666666\6666-main\db\backup-utf8.zip
   ```

## 📥 الطريقة 2: استخدام Git (إذا كان مثبتاً)

```powershell
cd F:\666666\6666-main
git checkout main
git pull origin main
```

ثم سيكون الملف موجوداً في:
```
F:\666666\6666-main\db\backup-utf8.zip
```

## 📥 الطريقة 3: استخدام PowerShell مع Proxy (إذا لزم الأمر)

```powershell
# إذا كنت تستخدم proxy
$proxy = "http://proxy.example.com:8080"
$webClient = New-Object System.Net.WebClient
$webClient.Proxy = New-Object System.Net.WebProxy($proxy)
$webClient.DownloadFile("https://github.com/alabasi2025/6666/raw/main/db/backup-utf8.zip", "db\backup-utf8.zip")
```

## ✅ بعد التحميل

بعد تحميل الملف، سأقوم بـ:
1. استخراج الملف المضغوط
2. استيراد النسخة الاحتياطية إلى قاعدة البيانات `666666`
3. التحقق من الجداول والبيانات

---

**ملاحظة**: إذا كان الملف موجوداً بالفعل في مكان آخر، أخبرني بالمسار وسأقوم بنسخه.

