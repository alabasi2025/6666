# 🔐 بيانات الدخول - Database Credentials

**تاريخ التحديث:** الجمعة، 10 يناير 2026

---

## 🗄️ **قاعدة البيانات PostgreSQL**

### إعدادات الاتصال:
```
المستخدم: postgres
كلمة المرور: 774424555
قاعدة البيانات: 666666
المنفذ: 5433 (PostgreSQL 18)
المضيف: localhost
```

### Connection String:
```
DATABASE_URL=postgresql://postgres:774424555@localhost:5433/666666
```

---

## 👤 **مستخدمي النظام (Application Users)**

### المستخدم الافتراضي (Admin):

من ملف `server/auth.ts` (دالة `ensureDefaultAdmin`):

```
📱 رقم الهاتف: 0500000000
🔑 كلمة المرور: 123456
👤 الدور: super_admin
📛 الاسم: مدير النظام
```

**ملاحظة:** يمكن تخصيص هذه القيم من خلال متغيرات البيئة:
- `DEFAULT_ADMIN_PHONE` - رقم الهاتف (افتراضي: 0500000000)
- `DEFAULT_ADMIN_PASSWORD` - كلمة المرور (افتراضي: 123456)
- `DEFAULT_ADMIN_NAME` - الاسم (افتراضي: مدير النظام)

---

## 📋 **المستخدمون الموجودون حالياً**

### للتحقق من المستخدمين الموجودين:
```powershell
cd f:\666666\6666-main
pnpm tsx check-users.ts
```

---

## 🔄 **إنشاء مستخدم جديد**

### الطريقة 1: من خلال السيرفر
المستخدم الافتراضي يتم إنشاؤه تلقائياً عند بدء السيرفر إذا لم يكن موجوداً.

### الطريقة 2: من خلال قاعدة البيانات مباشرة
```sql
-- في PostgreSQL
INSERT INTO users (open_id, phone, password, name, role, is_active, login_method)
VALUES (
  'local_0500000000_' || EXTRACT(EPOCH FROM NOW()),
  '0500000000',
  '$2b$10$...', -- كلمة المرور المشفرة باستخدام bcrypt
  'مدير النظام',
  'super_admin',
  true,
  'local'
);
```

### الطريقة 3: استخدام سكريبت Node.js
```typescript
import { registerUser } from './server/auth';

const result = await registerUser({
  phone: '0500000000',
  password: '123456',
  name: 'مدير النظام',
  role: 'super_admin'
});
```

---

## 🔐 **تشفير كلمة المرور**

كلمات المرور مشفرة باستخدام `bcrypt` مع `SALT_ROUNDS = 10`.

### لتشفير كلمة مرور جديدة:
```typescript
import { hashPassword } from './server/auth';

const hashedPassword = await hashPassword('123456');
```

---

## ⚠️ **ملاحظات أمنية**

1. **لا تكشف كلمات المرور في الكود:**
   - استخدم متغيرات البيئة للقيم الحساسة
   - لا تضع كلمات المرور في ملفات الكود

2. **غيّر القيم الافتراضية في الإنتاج:**
   - غيّر كلمة مرور قاعدة البيانات PostgreSQL
   - غيّر بيانات المستخدم الافتراضي

3. **استخدم كلمات مرور قوية:**
   - على الأقل 8 أحرف
   - تحتوي على أحرف كبيرة وصغيرة وأرقام ورموز

---

## 📝 **ملف .env المقترح**

```env
# قاعدة البيانات PostgreSQL
DATABASE_URL=postgresql://postgres:774424555@localhost:5433/666666

# المستخدم الافتراضي (اختياري)
DEFAULT_ADMIN_PHONE=0500000000
DEFAULT_ADMIN_PASSWORD=123456
DEFAULT_ADMIN_NAME=مدير النظام

# إعدادات أخرى
DEMO_MODE=false
PORT=8000
NODE_ENV=development
```

---

## ✅ **الخلاصة**

### قاعدة البيانات PostgreSQL:
- **المستخدم:** `postgres`
- **كلمة المرور:** `774424555`

### مستخدم النظام (Admin):
- **رقم الهاتف:** `0500000000`
- **كلمة المرور:** `123456` (الافتراضي)
- **الدور:** `super_admin`

---

**تم إنشاء الملف:** الجمعة، 10 يناير 2026
