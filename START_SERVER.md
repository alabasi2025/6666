# 🚀 تعليمات تشغيل النظام - Server Startup Guide

**تاريخ:** 6 يناير 2026

---

## 📋 **الطريقة 1: تشغيل يدوي (موصى به)**

### **في PowerShell:**

```powershell
# 1. الانتقال للمجلد
cd F:\666666\6666-main

# 2. تعيين المتغيرات البيئية
$env:PORT='8000'
$env:NODE_ENV='development'

# 3. تشغيل السيرفر
npm run dev
```

### **في Command Prompt:**

```cmd
cd F:\666666\6666-main
set PORT=8000
set NODE_ENV=development
npm run dev
```

---

## 📋 **الطريقة 2: استخدام ملفات التشغيل**

### **Windows:**

```powershell
# من مجلد المشروع
.\start-dev.ps1
```

أو:

```powershell
.\start-demo.ps1
```

---

## ⏱️ **الانتظار**

بعد تشغيل الأمر، انتظر حتى ترى رسالة مثل:

```
✓ Server started on port 8000
✓ Vite server ready
```

عادة يستغرق 10-30 ثانية.

---

## 🌐 **الوصول للنظام**

بعد بدء السيرفر، افتح المتصفح على:

```
http://localhost:8000
```

---

## 🔍 **التحقق من التشغيل**

### **1. تحقق من المنفذ:**

```powershell
netstat -ano | findstr :8000
```

يجب أن ترى عملية Node.js تستمع على المنفذ 8000.

### **2. تحقق من Health Check:**

افتح في المتصفح:
```
http://localhost:8000/health
```

يجب أن ترى JSON response.

---

## 🐛 **استكشاف الأخطاء**

### **المشكلة: المنفذ مشغول**

```powershell
# إيقاف جميع عمليات Node.js
Get-Process | Where-Object {$_.ProcessName -eq "node"} | Stop-Process -Force
```

### **المشكلة: خطأ في التبعيات**

```powershell
# إعادة تثبيت التبعيات
npm install
```

### **المشكلة: خطأ في قاعدة البيانات**

تأكد من:
- وجود ملف `.env` مع `DATABASE_URL` أو `DEMO_MODE=true`
- قاعدة البيانات تعمل (إن كنت تستخدمها)

---

## 📝 **ملاحظات مهمة**

1. **السيرفر يجمع Backend + Frontend:**
   - Backend: Express على المنفذ 8000
   - Frontend: Vite dev server (proxy عبر Express)

2. **في وضع التطوير:**
   - Hot reload مفعل
   - التغييرات تظهر تلقائياً

3. **DEMO_MODE:**
   - إذا كان `DEMO_MODE=true` في `.env`، يمكنك الدخول بدون قاعدة بيانات

---

## ✅ **بعد التشغيل الناجح**

1. افتح `http://localhost:8000`
2. سجل الدخول (أو استخدم DEMO_MODE)
3. اذهب إلى: **المحركات الأساسية** من التبويب الجانبي
4. اختبر الشاشات:
   - محرك التسعير
   - محرك التسوية
   - محرك الجدولة
   - محرك الإسناد
   - فحص الصحة

---

**آخر تحديث:** 6 يناير 2026

