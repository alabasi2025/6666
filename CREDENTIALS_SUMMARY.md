# 🔐 ملخص بيانات الدخول - Credentials Summary

**تاريخ التحديث:** الجمعة، 10 يناير 2026

---

## 🗄️ **قاعدة البيانات PostgreSQL**

### بيانات الاتصال:
```
👤 المستخدم: postgres
🔑 كلمة المرور: 774424555
🗄️  قاعدة البيانات: 666666
📍 المنفذ: 5433 (PostgreSQL 18)
🌐 المضيف: localhost
```

### Connection String:
```
postgresql://postgres:774424555@localhost:5433/666666
```

---

## 👤 **مستخدم النظام (Application User)**

### المستخدم الافتراضي (Admin):

```
📱 رقم الهاتف: 0500000000
🔑 كلمة المرور: 123456
👔 الدور: super_admin
📛 الاسم: مدير النظام
```

### معلومات إضافية:
- **الإنشاء:** تلقائياً عند بدء السيرفر (إذا لم يكن موجوداً)
- **التخصيص:** يمكن تغييره من خلال متغيرات البيئة (`.env`)
  - `DEFAULT_ADMIN_PHONE` - رقم الهاتف
  - `DEFAULT_ADMIN_PASSWORD` - كلمة المرور
  - `DEFAULT_ADMIN_NAME` - الاسم

---

## 📋 **التحقق من المستخدمين**

### فحص المستخدمين الموجودين:
```powershell
cd f:\666666\6666-main
pnpm tsx check-users.ts
```

### إنشاء مستخدم مدير جديد:
```powershell
cd f:\666666\6666-main
$env:DATABASE_URL="postgresql://postgres:774424555@localhost:5433/666666"
pnpm tsx create-default-admin.ts
```

---

## 🚀 **تسجيل الدخول**

### 1. افتح المتصفح:
```
http://localhost:8000
```

### 2. استخدم بيانات الدخول:
- **رقم الهاتف:** `0500000000`
- **كلمة المرور:** `123456`

### 3. بعد تسجيل الدخول:
- ستكون مدير النظام (super_admin)
- يمكنك الوصول لجميع الميزات
- يمكنك إنشاء مستخدمين جدد

---

## 🔄 **إنشاء مستخدمين جدد**

### من خلال الواجهة:
1. سجل الدخول كمدير
2. اذهب إلى إدارة المستخدمين
3. أنشئ مستخدم جديد

### من خلال الكود:
```typescript
import { registerUser } from './server/auth';

const result = await registerUser({
  phone: '0501234567',
  password: 'password123',
  name: 'اسم المستخدم',
  role: 'user' // أو 'admin', 'super_admin'
});
```

---

## ⚠️ **ملاحظات أمنية مهمة**

### 1. في بيئة التطوير (Development):
- ✅ يمكن استخدام القيم الافتراضية
- ⚠️ لا تكشف هذه القيم في Git

### 2. في بيئة الإنتاج (Production):
- ❌ **يجب تغيير جميع القيم الافتراضية:**
  - كلمة مرور قاعدة البيانات PostgreSQL
  - رقم الهاتف وكلمة مرور المستخدم الافتراضي
  - جميع المتغيرات الحساسة

### 3. أفضل الممارسات:
- استخدم كلمات مرور قوية (12+ حرف، أرقام، رموز)
- استخدم متغيرات البيئة للقيم الحساسة
- لا تضع كلمات المرور في ملفات الكود
- استخدم `.env` ملف محمي (في .gitignore)

---

## 📝 **ملف .env الموصى به**

```env
# قاعدة البيانات PostgreSQL
DATABASE_URL=postgresql://postgres:774424555@localhost:5433/666666

# المستخدم الافتراضي (اختياري - للبيئة التطويرية فقط)
DEFAULT_ADMIN_PHONE=0500000000
DEFAULT_ADMIN_PASSWORD=123456
DEFAULT_ADMIN_NAME=مدير النظام

# إعدادات النظام
DEMO_MODE=false
PORT=8000
NODE_ENV=development

# الأمان (للإنتاج)
SESSION_SECRET=your-strong-secret-key-here-min-32-chars
```

---

## ✅ **الخلاصة السريعة**

### قاعدة البيانات:
- **المستخدم:** `postgres`
- **كلمة المرور:** `774424555`

### مستخدم النظام:
- **رقم الهاتف:** `0500000000`
- **كلمة المرور:** `123456`
- **الدور:** `super_admin`

---

**تم إنشاء الملف:** الجمعة، 10 يناير 2026  
**الحالة:** ✅ جاهز للاستخدام
