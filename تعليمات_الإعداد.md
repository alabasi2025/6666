# تعليمات إعداد وتشغيل المشروع

## ✅ ما تم إنجازه

1. ✅ تم استنساخ المشروع من GitHub
2. ✅ تم إنشاء ملف `.env` مع الإعدادات الأساسية

## ⚠️ المتطلبات المطلوبة

قبل تشغيل المشروع، يجب تثبيت الأدوات التالية:

### 1. Node.js
- **الرابط**: https://nodejs.org/
- **الإصدار المطلوب**: v18 أو أحدث (يفضل v20+)
- **كيفية التثبيت**: 
  - حمّل المثبت من الموقع الرسمي
  - شغّل المثبت واتبع التعليمات
  - تأكد من اختيار "Add to PATH" أثناء التثبيت

### 2. pnpm (مدير الحزم)
بعد تثبيت Node.js، افتح PowerShell وثبت pnpm:

```powershell
npm install -g pnpm
```

للتحقق من التثبيت:
```powershell
pnpm --version
```

### 3. MySQL Database
- **الرابط**: https://dev.mysql.com/downloads/mysql/
- **الإصدار المطلوب**: MySQL 8.0 أو أحدث
- **كيفية التثبيت**:
  - حمّل MySQL Community Server
  - شغّل المثبت
  - احفظ كلمة المرور الجذر (root password) التي ستحددها

## 📝 خطوات الإعداد

### الخطوة 1: تحديث ملف `.env`

افتح ملف `.env` في مجلد المشروع وعدّل الإعدادات التالية:

```env
# قاعدة البيانات - عدّل هذه القيم حسب إعدادات MySQL الخاصة بك
DATABASE_URL=mysql://root:YOUR_PASSWORD@localhost:3306/energy_management

# إعدادات الخادم
PORT=5000
HOST=0.0.0.0
NODE_ENV=development

# إعدادات المدير الافتراضي
DEFAULT_ADMIN_PHONE=0500000000
DEFAULT_ADMIN_PASSWORD=Admin@123456
DEFAULT_ADMIN_NAME=مدير النظام

# مستوى التسجيل
LOG_LEVEL=info

# إعدادات الجلسة والمصادقة
JWT_SECRET=dev-secret-key-change-in-production-12345678
SESSION_SECRET=dev_session_secret_change_in_production_12345678
```

**مهم**: استبدل `YOUR_PASSWORD` بكلمة مرور MySQL الخاصة بك.

### الخطوة 2: إنشاء قاعدة البيانات

افتح MySQL Command Line Client أو أي أداة إدارة MySQL (مثل MySQL Workbench) وقم بتنفيذ:

```sql
CREATE DATABASE IF NOT EXISTS energy_management CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
```

### الخطوة 3: تثبيت الاعتماديات

افتح PowerShell في مجلد المشروع (`F:\666666\6666-main`) وقم بتنفيذ:

```powershell
pnpm install
```

هذه العملية قد تستغرق عدة دقائق.

### الخطوة 4: إعداد قاعدة البيانات (Database Migration)

```powershell
pnpm db:push
```

هذا الأمر سينشئ جميع الجداول المطلوبة في قاعدة البيانات.

### الخطوة 5: تشغيل المشروع

```powershell
pnpm dev
```

بعد التشغيل، يجب أن ترى رسالة مثل:
```
Server running on http://0.0.0.0:5000/
```

### الخطوة 6: فتح المتصفح

افتح المتصفح وانتقل إلى:
```
http://localhost:5000
```

## 🔐 تسجيل الدخول

استخدم البيانات الافتراضية:
- **الهاتف**: 0500000000
- **كلمة المرور**: Admin@123456

## 🛠️ استكشاف الأخطاء

### المشكلة: `pnpm: command not found`
**الحل**: تأكد من تثبيت Node.js ثم قم بتثبيت pnpm:
```powershell
npm install -g pnpm
```

### المشكلة: `Cannot connect to MySQL`
**الحل**: 
1. تأكد من تشغيل خدمة MySQL
2. تحقق من صحة بيانات الاتصال في ملف `.env`
3. تأكد من إنشاء قاعدة البيانات `energy_management`

### المشكلة: `Port 5000 is already in use`
**الحل**: 
- غيّر المنفذ في ملف `.env` إلى منفذ آخر (مثل 5001)
- أو أوقف التطبيق الذي يستخدم المنفذ 5000

### المشكلة: `Database migration failed`
**الحل**:
1. تأكد من اتصال قاعدة البيانات
2. تحقق من صلاحيات المستخدم (root)
3. تأكد من وجود قاعدة البيانات `energy_management`

## 📚 ملفات مفيدة

- `SYSTEM_STARTUP_REPORT.md` - تقرير شامل عن النظام
- `USER_DEPLOYMENT_GUIDE.md` - دليل النشر للمستخدم
- `DEPLOYMENT_INSTRUCTIONS.md` - تعليمات النشر التفصيلية

## 🎯 الخطوات التالية بعد التشغيل

1. ✅ تسجيل الدخول باستخدام الحساب الافتراضي
2. ✅ إضافة العملات الأساسية (SAR, USD, EUR)
3. ✅ إنشاء الحسابات الرئيسية
4. ✅ تعيين أسعار الصرف
5. ✅ اختبار النظام المخصص v2.2.0

## 📞 الدعم

إذا واجهت أي مشاكل، راجع:
- ملفات التوثيق في مجلد `docs/`
- ملفات التقارير في المجلد الرئيسي
- ملف `TESTING_CHECKLIST.md` للتحقق من الخطوات

---

**تاريخ الإنشاء**: 2 يناير 2026  
**الإصدار**: 2.2.0

